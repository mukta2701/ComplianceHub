# SDD ledger — plan: docs/superpowers/plans/2026-08-24-github-compliance-integration.md

## Pre-flight scan

| Task/interface | Produces | Consumer/shared surface | Finding / ruling |
| --- | --- | --- | --- |
| Task 1 | Canonical mapping types, checksum, standard pack | Tasks 2, 3, 4, 5 | Clean. Task 1 is the single TypeScript source of mapping semantics; SQL seed values must match its literal reviewed payload/checksum. |
| Task 2 | Approval/provenance schema and materialisation RPC | Tasks 3–5, 8 | Clean. The RPC owns atomic evidence/finding lifecycle; application code must not recreate transaction logic. |
| Task 3 | Post-terminal-run application boundary | Tasks 4, 8 and daily reconciliation | Clean. Preserve `runGitHubCollection` isolation; materialisation is invoked separately after terminal collection. |
| Task 4 | Approval and official-result web UI | Tasks 5 and 8 | Clean. UI consumes stable provenance returned by Tasks 2–3 and never derives compliance from raw shadow rows. |
| Task 5 | Versioned MCP bundle/tool/digest facts | Tasks 6 and 8 | Clean. It consumes official records only; Task 6 changes delivery eligibility but not facts/reservation semantics. |
| Task 6 | Server-side Slack destination allow-policy | Tasks 7–8 | Clean. Use exact webhook SHA-256 identity; labels/channel names are never identity. |
| Task 7 | Restart-safe pilot and production release aggregate | Task 8 | Clean. It supplies the verified local-only environment and production server used by acceptance. |
| Task 8 | End-to-end proof and evidence document | Task 9 | Clean. Live network calls are replaced only at GitHub/Slack transport boundaries; all internal logic and persistence stay real. |
| Task 9 | Whole-branch review and fix wave | Final handoff | Clean. External writes remain outside implementation and require action-time approval. |
| Tasks 2 and 5 | Database migration/version attestation | Deployment workflow/docs/contracts | Potential conflict resolved: if Task 5 requires a successor migration after Task 2 is committed, Task 5 must create it via CLI and again advance every attestation. Never edit a committed migration. |
| Tasks 4 and 8 | Same Playwright fixture | UI acceptance | Clean. Task 4 establishes the visible materialisation proof; Task 8 extends it to MCP/digest and replay/fresh-pass lifecycle without bypassing Task 4 behaviour. |
| Tasks 6 and 8 | Real-destination policy vs loopback proof | Digest delivery | Ruling: production validation remains exact Slack HTTPS plus allow-hash; the loopback receiver exists only as an injected test transport under `NODE_ENV=test`. Cost if wrong: a test-only branch could become a delivery bypass, so final review and bundle/runtime scans must explicitly verify it is unreachable in production. |

Baseline: branch `codex/compliancehub-internal-tool` at `be0d0a1`; lint, typecheck, and 210 Vitest files / 1,472 tests passed. Production build was not a code failure: Turbopack rejected the temporary `node_modules` symlink outside the clone root. Final verification requires a real dependency tree.

Ruling: Mapping entries store reviewed ISO requirement codes (for example `A.8.32`), while evidence links resolve them through `requirements` and `requirement_control_mappings` to the shared `controls.id`; they must not pretend the internal `CH-*` control code is an ISO reference. This preserves the catalogue model. Cost if wrong: a missing requirement mapping must fail materialisation rather than create unlinked evidence.

Ruling: Task 7 must replace runtime Google-font fetching with repository-owned local Geist/Geist Mono assets and `next/font/local`, preserving typography while making clean production builds offline/reproducible. Cost if wrong: copied assets or weights could drift from the intended UI, so desktop/mobile visual and bundle verification must cover the change.

Task 1: minor (deferred): failure severity/remediation appears in both evaluator results and mapping definitions; final review must verify the distinction between observed rule output and approved compliance-mapping policy is explicit and drift-safe.

Task 1: fix round 1/5 (1 addressed, 0 open — treatment-kind invariants enforced for pass/fail/unknown/not-applicable; commit 626b959)
Task 1: complete (commits 7bcf932..626b959, review clean)

Task 2: Ruling: the generated `20260824184628` migration is not a released historical migration until Task 2 passes review; fix rounds must amend this migration rather than stack corrective migrations on unverified SQL. This is the narrow exception to the no-editing-committed-migrations constraint. Cost if wrong: another consumer could have applied the reviewed-but-unreleased commit, so release notes must continue to say it is pending and final DB reset/upgrade tests are mandatory.
Task 2: Ruling: expanded monitoring states must remain visible immediately after the schema migration, not wait for Tasks 4–5. The Task 2 fix may touch the existing monitoring loaders/page schemas and replace the MCP bundle function; Tasks 4–5 will add richer controls/presentation later. Cost if wrong: intermediate extra scope may duplicate later work, but avoids silently hiding failed conditions.
Task 2: minor (deferred): treatment JSON validation must reject nested JSON nulls explicitly rather than allow SQL NULL through a CHECK.
Task 2: minor (deferred): concurrency pgTAP currently serializes on organisation approval before the identity lock, and revocation-denial coverage lacks the full Admin/outsider/dual-workspace matrix; final review must demand stronger direct evidence.
Task 2: fix round 1/5 (5 addressed, 2 new Important open — mapping-version finding split; future pack construction blocked; commits 5595a4e..0223fcc)
Task 2: Ruling: the approved spec overrides the plan's mistaken mapping-version component in the finding uniqueness sentence. A technical condition remains one finding across mapping-pack versions; provenance/audit retains the mapping version used for each observation and a new approval may change presentation/mapped controls without duplicating or orphaning the condition. Cost if wrong: a materially changed mapping policy could deserve a separate business workflow, but duplicating one live technical failure is less trustworthy and contradicts the approved finding key.
Task 2: fix round 2/5 (2 addressed, 1 new Important open — global pack seal exposed to service_role without Owner attribution; commit 0223fcc..a5652bb)
Task 2: Ruling: publishing a global standard mapping-pack version is release/migration work, not a workspace Owner decision. The sealing helper remains postgres-only with no Data API grant; workspace Owner accountability begins at the separate audited approval RPC. Cost if wrong: operators cannot publish a new standard pack dynamically through the app, but the approved design requires review/versioned release content rather than ad-hoc runtime authoring.
Task 2: fix round 3/5 (1 addressed, 0 open — global seal is postgres-only; commit a5652bb..baccb69)
Task 2: verification pending: SQL compilation, pgTAP, upgrade, and two-session concurrency cannot run because this sandbox denies the Colima Docker socket. Static spec/quality review is clean; this remains a mandatory final release gate, not a completion claim.
Task 2: complete (commits 626b959..baccb69, review clean; database runtime gate pending final verification)
Task 2: Ruling: automatic materialisation is authorized by the active immutable workspace mapping approval, not by inventing a human actor for each scheduled run. The server-only materialiser derives approval lineage/`approved_by` internally and remains callable only by `service_role`; Owner revalidation is required for approval/revocation and human transitions, not for the approved automated process. Cost if wrong: a previously approved pack continues operating after its original approver is demoted until a current Owner revokes it, which is the intended durable-configuration model but requires visible approval/revocation UI.
Task 2: reopened before Task 3 — load-bearing interface defect: scheduled reconciliation has no truthful `target_actor_id`, while the reviewed RPC requires and revalidates one.
Task 2: fix round 4/5 (1 controller finding addressed, 0 open — active approval authorizes actorless service materialisation; commit baccb69..672954d)
Task 2: complete (commits 626b959..672954d, static review clean; database runtime gate remains mandatory)
Task 3: Ruling: the plan incorrectly names only the generic `/api/cron/daily` route for post-collection materialisation. The primary invocation belongs after the dedicated GitHub scheduled/manual/webhook collection orchestration, with the generic daily cron acting only as a bounded recovery sweep. `runGitHubCollection` itself remains phase-1-only. Cost if wrong: more call sites must share one idempotent materialisation service, but official records appear promptly and retries remain independent.
Task 3: minor (deferred): repeat/concurrency unit coverage currently mocks one `unchanged` response instead of racing/repeating the real application boundary; final review must require behavioural delegation evidence.
Task 3: Ruling: bounded fair recovery requires a durable per-run materialisation job/attempt state, not repeatedly sampling newest terminal runs. Task 3 may create a successor migration/pgTAP test via the Supabase CLI and must advance deployment attestation again; Task 2's reviewed migration remains unchanged. Cost if wrong: an additional table/trigger increases schema surface, but without it transient or awaiting-approval runs can starve indefinitely and the route can falsely report healthy.
Task 3: fix round 1/5 (prior Critical sampling starvation and Important 15-check validator addressed; 3 new Important open — approval/finalisation missed wake race, mixed deferred webhook primary reconciliation skip, retry ceiling without visible exhausted state; commit 2674155..1698dc1)
Task 3: Ruling: the attempt ceiling is a safety control, not permission to make unfinished work disappear. Exhausted materialisation must become a durable visible attention state and exact primary reconciliation must account for requested-but-unclaimable jobs. Cost if wrong: operators may need an explicit guarded requeue workflow later, but health remains truthful and automatic retries remain bounded.
Task 3: fix round 2/5 (3 prior Important addressed; 1 new Important open — repeated awaiting-approval claims could decrement historical attempts; commit 1698dc1..0d50e37)
Task 3: fix round 3/5 (attempt accounting made counter-neutral while awaiting approval and real attempts remain bounded; 0 Critical/Important open; commit 0d50e37..d179300)
Task 3: minor (deferred): pgTAP does not explicitly finalise a counter-neutral lease with completed/retryable, although the shared effective-attempt branch is static and the adapter covers zero-counter transport.
Task 3: verification pending: SQL compilation, pgTAP 071, and real advisory-lock/lease concurrency cannot run because this sandbox denies the Colima Docker socket. Full unit/lint/typecheck/actionlint/privacy checks and static review are clean; database runtime remains a mandatory final gate.
Task 3: complete (commits 672954d..d179300, static spec/quality review clean; database runtime gate pending final verification)
Task 5 execution ruling: user reprioritised the data pipeline ahead of new web UI, so original Task 5 is split into separately reviewed Phase 2 (Task 5A official-result ledger + read-only MCP tool) and Phase 3 (Task 5B bundle/digest v2). Task 4 UI follows those phases. Cost if wrong: two successor migrations/attestation updates instead of one, but the official read contract and delivery-content contract get independent atomic reviews.
Task 5A ruling: never reconstruct an "official" GitHub result from shadow observations plus the currently active approval. Persist immutable per-observation official outcomes transactionally with materialisation, including unknown/not-applicable/stale, then expose only those rows through an RLS-scoped versioned read RPC. Cost if wrong: an extra immutable ledger and RPC increase schema surface, but historical pack attribution and all four outcomes remain truthful.
Task 5A: fix round 1/5 (provider/account label disclosure, latest-before-filter, failed-run no-op, idempotency annotation, parser strictness addressed; behavioral pgTAP and replay chronology/lineage remained open; commits 5c3863e..701a945)
Task 5A: fix round 2/5 (absolute-time/truncation/order and immutable finding-lineage replay addressed; behavioral pgTAP remained open; commit 9cda217)
Task 5A: implementation attempts 3-4 failed cleanly on the required behavioral DB matrix; no product commits from those attempts. Fresh database specialist replaced the structural-only test.
Task 5A: fix round 3/5 (78-assertion direct behavior/RLS/outcome matrix added; independent review found offset ordering, reachable-pack, failed-run lifecycle, concurrency and cleanup gaps; commits 53d99a1..cf5a12d)
Task 5A: fix round 4/5 (epoch ordering, real dblink wrapper race, real complete sealed mapping pack, failed/rate-limited FAIL no-effects, and successor-schema cleanup ordering addressed; 0 Critical/Important open; commits cf5a12d..98f4572)
Task 5A: verification pending: migrations/pgTAP 070/072/upgrade/two-session execution cannot run because local PostgreSQL/Docker is inaccessible in this sandbox. Full 212-file/1,551-test suite, typecheck, lint, actionlint, privacy and static review are clean; DB runtime remains a mandatory final gate.
Task 5A: complete (commits d179300..98f4572, static spec/security/quality review approved; database runtime gate pending final verification)
Task 5A: fix round 1/5 (5 addressed, 3 open — pgTAP behavioral matrix; valid truncation/equal-time ordering parser breakage; exact immutable finding replay lineage; commits 5c3863e..701a945)
Task 5A: fix round 2/5 (3 addressed, 1 open — pgTAP 072 direct behavioral role/outcome/lifecycle/concurrency matrix; commits 701a945..9cda217)
Task 5A: fix round 3/5 (0 addressed, 1 open — pgTAP 072 full direct behavioral matrix; no commit, two partial role assertions left for fresh round-4 owner)
Task 5A: fix round 4/5 (0 addressed, 1 open — fresh escalated implementer designed but did not write the pgTAP 072 direct behavioral matrix; no commit)
Task 5B: ruling: schema-v2 mapping status follows Task 5A mapping identity rather than approval-row identity. Revoking and reapproving the exact same pack/version/checksum keeps an immutable result active; only a changed mapping identity is historical. Lifecycle change provenance still binds to the result's original approval. Cost if wrong: approval audit chronology remains separate from technical mapping currency, but a no-op reapproval cannot falsely recategorise every unchanged result.
Task 5B: RED recorded for the absent schema-v2 bundle/facts/lines/retry/version contracts and for pass-type changes incorrectly accepting unknown/not-applicable outcomes. The strict successor pgTAP contract was written before SQL implementation; database runtime RED/GREEN is unavailable because Postgres cannot be reached.
Task 5B: static implementation ready for independent review. Generated `20260825040825_mcp_github_digest_v2.sql`; added exact 43-assertion direct-behavior `073`; upgraded canonical digest facts/hash, closed-world wording, schema-transition retry safety, plugin/server `0.2.0`, integration contract, and all 18-migration attestations. Focused tests, full Vitest exit, typecheck, lint, actionlint, and diff checks are green.
Task 5B: runtime verification pending: Docker socket denial blocks reset/073/all-DB/upgrade/two-session execution; the disposable localhost Supabase integration environment is absent; the production build is blocked by the pre-existing Google-font network dependency. Do not mark Phase 3 runtime-complete until these gates and independent review pass.
Task 5B: independent-review fix round 1: confirmed the two Important findings and one Minor contract defect. A narrow authenticated membership-scoped security-definer helper now exposes only prior delivered date/time so Owner/Admin/Member receive the same safe baseline and verified deltas while current delivery metadata stays Owner-only. Delta derivation now uses every exact-active-mapping official event rather than the latest-state collapse; direct fixtures cover fail→unchanged fail, fail→pass, and fail→pass→fail with retained lifecycle events, latest state, deterministic ordering, and repeat reads. Schema-v2 fact construction now requires GitHub input at compile time and runtime. Exact `073` plan is 60/60 labels. Focused 147 tests, full Vitest, typecheck, lint, actionlint, and diff checks exited 0; Docker/pgTAP, disposable integration environment, and Google-font build gaps remain unchanged. Phase 3 remains static-only pending database runtime and independent re-review.
Task 5B: independent-review final cleanup: strict parser and domain normalization now reject every change whose official `materialisedAt` is at or before the delivered baseline, while deliberately allowing an earlier `occurredAt`/observation for delayed materialisation. RED was exactly two intended failures with 72 passing; focused GREEN is 74/74 with typecheck, lint, and diff checks green. No database/schema, delivery, transport, version, or attestation surface changed; prior environmental runtime gaps remain unchanged.
Task 6: RED recorded for the absent exact Slack destination policy and for action/daily/monitoring/deployment/plugin boundaries that still accepted Owner-selected or stored destinations without the server allow digest. The expanded boundary RED had 20 intended failures with 102 passing; the Azure/plugin RED had five intended failures with 11 passing. Focused pgTAP `074` reached the database boundary and failed to connect before assertions.
Task 6: ruling: the application user client must approve the selected encrypted row and hold only its exact channel ID plus canonical URL before any service-role construction. The successor reservation RPC independently locks and binds that expected active selected channel before delivery/attempt/audit mutation; the current server allow digest is rechecked immediately before the shared transport. Cost if wrong: a destination switch after a terminal delivered/ambiguous state cannot change its durable duplicate classification, but every new or confirmed-failure send path remains exact-channel bound.
Task 6: static implementation complete. Generated `20260825053718_restrict_slack_delivery_destination.sql`; added exact 31-assertion pgTAP `074`; applied Owner-only fail-closed setup/enable/select, daily expected-channel reservation, isolated monitoring policy, Azure A/B/Bicep/env/docs/plugin contracts, and invalidated all prior Ankit/AdTecher/KT-SME Slack acceptance evidence. Focused 156 tests, full 213-file/1,593-test Vitest, lint, typecheck, actionlint, Bicep compile, source/static scans, and diff checks are green; the privacy hook's exact false positives are recorded in the Task 6 report.
Task 6: runtime verification pending. Local PostgreSQL/Docker and disposable integration credentials are unavailable; the safe upgrade harness refused without destructive opt-in; `npm run verify` passed lint/typecheck/1,593 tests then the production build hit the pre-existing network-blocked Google Fonts dependency. No provider/hosted/Azure/Slack writes occurred. Task 6 must not be called runtime/hosted accepted until DB/integration/build gates and Mukta-owned destination proof pass.
Task 6: fix round 1 ruling supersedes the old-signature-drop decision. Migration `20260825053718` is additive and temporarily retains both service-only overloads. The application defaults to strict and permits one exact-`PGRST202` legacy fallback only during an explicit manual bridge. Azure must deploy manual bridge on `20260825040825`, then additive `20260825053718`, then manual final/strict; automatic deployments are final/strict and final requires the exact policy-capable bridge revision. Cost if wrong: the legacy service-only surface remains until a later retirement migration, but this preserves a reversible hosted upgrade without weakening browser/Data API denial.
Task 6: fix round 1 RED recorded: digest/errors/health/Azure focused run had 21 intended failures and 78 passing tests before product edits. Final verification evidence and remaining runtime gates are pending.
Task 6: fix round 1 static GREEN. Focused digest/error/health/Azure/plugin contracts passed 6 files/107 tests; `npm run verify` passed lint, typecheck, and full 213-file/1,619-test Vitest before the unchanged Google Fonts network build failure; Actionlint, Bicep, diff, privacy/source/static scans passed. pgTAP/full DB still cannot connect to local Postgres, upgrade still refuses without destructive opt-in, and integration still lacks disposable localhost credentials. No external write occurred; hosted bridge/final, database runtime, production build, and Mukta-owned destination acceptance remain open.
