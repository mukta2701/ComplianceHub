# Milestone 2 baseline

Recorded 23 September 2026 before implementation of individual mapping review. This baseline covers repository source and synthetic test fixtures. It does not identify live workspaces, repositories, users, or approval rows.

## Source and scope

- Worktree: `codex/m2-monitoring-design` at `0788a679d89d0dbaaff88f1ea1b913f6e0e819ed`.
- The worktree was clean before this evidence file. Application source is unchanged from the branch base.
- The evaluator defines 15 deterministic GitHub check IDs in [`rules.ts`](../../src/features/github/domain/rules.ts). The static ISO mapping pack contains 15 entries in [`mapping.ts`](../../src/features/github/domain/mapping.ts).
- Repository selection is represented by `github_repositories.selected`. The Owner-only selection function and Monitoring readers are present in the source migrations and use selected repositories. This source review does not confirm which repository, if any, is selected in a live workspace.
- Mapping review and materialisation support pack-level approval today. Materialisation loads one active, published pack approval and requires observations for all 15 checks. Per-entry decisions remain to be implemented. The code lives in [`github-mapping-review.ts`](../../src/features/github/application/github-mapping-review.ts) and [`materialise-approved-observations.ts`](../../src/features/github/application/materialise-approved-observations.ts).
- The database contract contains synthetic Owner, Admin, Member, and outside-workspace identities. I did not inspect persistent local account rows. The application can read active whole-pack approvals, but this baseline does not enumerate retained approval rows.
- Historical Monitoring screenshots exist, including [the 2 September desktop capture](phase1-monitoring-findings-desktop-2026-09-02.jpg) and [the 4 September Monitoring capture](phase3-monitoring-live-2026-09-04.png). Neither shows the planned per-entry review. No fresh baseline screenshot was taken.

## Local environment and migration ledger

A Node process answered on local port 3100. `GET http://127.0.0.1:3100/api/health/live` returned `status: ok` and `releaseSha: unknown`. No listener was found on ports 3200, 3300, or 3301. This health response does not identify the served source revision or demonstrate the Monitoring journey.

`node_modules/.bin/supabase migration list --local` exited 0. The source tree contains migrations through `20260910234820`. The preserved local application database ledger's latest applied migration is `20260818140000`; 37 source migrations after that are absent. This includes the GitHub compliance materialisation migrations. The database contract cannot run against that schema. No migration or reset was run against the preserved database.

After recording that mismatch, we started a separate disposable local Postgres project named `compliancehub-m2-isolated` on port 55432. Its work directory is `/tmp/compliancehub-m2-db.Qt1ZeX`, outside the repository. We applied the source migrations to this empty project only. This is a test database, not the local app database or AWS dev.

`curl -sS --max-time 5 http://127.0.0.1:3100/api/health/live` exited 0 and returned `status: ok` with `releaseSha: unknown`.

## Focused baseline checks

Each unit test ran alone through the resource guard, with one Vitest worker.

| Command | Result |
|---|---|
| `node --import=tsx scripts/local-resource-guard.ts -- npm run test -- --maxWorkers=1 src/features/github/domain/rules.test.ts` | Exit 0. 12 tests passed. |
| `node --import=tsx scripts/local-resource-guard.ts -- npm run test -- --maxWorkers=1 src/features/github/domain/mapping.test.ts` | Exit 0. 16 tests passed. |
| `node --import=tsx scripts/local-resource-guard.ts -- npm run test -- --maxWorkers=1 src/features/github/application/materialise-approved-observations.test.ts` | Exit 0. 50 tests passed. |
| `node --import=tsx scripts/local-resource-guard.ts -- npm run test:db -- --local supabase/tests/database/070_github_compliance_materialisation.sql` | Exit 1 before TAP tests started. The SQL fixture stopped at line 10 because `public.github_official_compliance_results` does not exist in the local schema. Zero subtests ran. |
| `node --import=tsx scripts/local-resource-guard.ts -- ./node_modules/.bin/supabase --workdir /tmp/compliancehub-m2-db.Qt1ZeX test db --local supabase/tests/database/070_github_compliance_materialisation.sql` | Exit 0 on the isolated database. All 179 TAP tests passed. |

The failed command used the default `--local` target. Its error matches the migration-ledger gap above. The successful command used the separate `--workdir` target; its result must not be attributed to the preserved local application database.

## Synthetic acceptance fixture contract

Use fictional workspaces `workspace-alpha` and `workspace-beta`, each with one selected repository. Add a third repository visible to the App but unselected in `workspace-alpha`.

| Case | Expected result |
|---|---|
| Selected repository in each workspace | Collect only those two repositories; never cross workspace boundaries. |
| App-visible but unselected repository | No collection or official result. |
| Observations | Cover Pass, Failure, Unknown, and Not applicable. |
| Changed mapping entry | Require a decision for the changed entry; preserve the earlier official record and approval ancestry. |
| Stale observation | Mark data older than the existing 36-hour freshness limit stale. |
| Duplicate collection | Replaying the same run creates no duplicate official records. |
| Failure followed by a newer Pass | Resolve the matching Finding only after the approved newer observation materialises. |

This written acceptance contract still needs an executable fixture. It contains no provider data or actual repository names.

## Gate

The rule, mapping, and TypeScript materialisation unit baselines pass on this source revision. The 179-test database contract also passes against a fresh, isolated database with the current source migrations. The preserved local application database remains 37 migrations behind and cannot demonstrate the new UI. Live selected-repository identity and active approval rows remain unknown because this baseline did not inspect private records.
