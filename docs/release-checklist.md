# Public beta release checklist

## Start here — plain-language project status

**Milestone 2 local baseline checked — 23 September 2026:** On the isolated planning branch, the existing 15-check rule tests (12), mapping tests (16) and materialisation tests (50) passed. The existing database contract passed 179 tests against a new disposable local Postgres instance with current source migrations. The preserved local application database is 37 migrations behind, so its attempted database test stopped before any TAP case; we did not migrate or reset it. [Baseline evidence](evidence/2026-09-23-m2-baseline.md). This is test evidence for the current whole-pack implementation, not an individual-review feature, a hosted Milestone 2 result or an accepted milestone.

**Milestone 2 design and plan recorded, no application change — 23 September 2026:** The Owner approved the product direction for trustworthy GitHub Monitoring: Owner-selected repositories, individual check-to-control review, deterministic official results, narrow daily and Owner/Admin rechecks, private-channel change alerts, built-in AI mapping proposals without a switch, 30-day Owner exceptions and safe Member summaries. The [design](plans/2026-09-23-trustworthy-monitoring-design.md) and [phased implementation plan](plans/2026-09-23-trustworthy-monitoring.md) compare that direction with the existing 15-check foundation. They do not implement or verify the missing entry-level review, daily schedule, compliance-change Slack alerts, AI mapping proposals or live Milestone 2 journey. The Milestone 1 AWS dev pilot remains unaccepted; its current status and evidence are on the active Milestone 1 branch. No Milestone 2 database, provider, AWS or production state changed.

**Core first-use journey and Settings cleanup is live on AWS dev — 22 September 2026:** The first incomplete setup step guides a new workspace through assessment, controls, risks, policy and team setup without replacing the approved dashboard. Assessment and Statement of Applicability creation use the shared role policy. The top-right account control now stays focused on signed-in identity, role, workspace switching and sign-out; the adjacent bell owns the notification inbox, while one built-in tone sounds only for notifications that arrive after the app is open. Settings no longer presents AI as a feature switch; company Codex access now sits under Connections. Customer trust is optional, private until an Owner or Admin publishes it, and no longer occupies primary navigation. Team invitations use a provider-free beta flow: an authorised operator creates a seven-day link, copies it once and shares it through an existing trusted channel; only the hash is stored, while expiry, email matching, acceptance and revocation remain enforced. Automatic invitation email remains a future provider decision. The leadership report now presents current residual risk and excludes closed risks, matching the risk register instead of reporting original inherent exposure. Monitoring was demonstrated locally in temporary Owner, Admin and Member roles: empty monitoring stays explicitly unconfirmed, Members remain read-only, and only Owners are invited to install GitHub or choose repositories. PR #25 passed its required GitHub checks and was squash-merged as `b067c80`; AWS deployment run `35728451603` completed successfully. The hosted health endpoint reports that exact release, and fresh authenticated checks confirmed the approved dashboard, Team settings, Connections, Monitoring and leadership-report screens on AWS dev. Fresh lint, type checking, production build and 3,169 unit checks pass, with 3 intentional skips. Astra High reviewed the link boundary and Luna Max traced the complete issue-to-accept path. Rendered desktop/mobile evidence is still required before a product-quality claim. GitHub integration work remains paused pending Aman.

**Azure staging removed, AWS only — 18 September 2026:** The old Azure staging deployment, its scheduled-maintenance workflow and its infrastructure files are deleted (merged as #15; live AWS dev release `63ff4a4` verified healthy). The `azure-staging` GitHub environment and its secrets are deleted; only `aws-dev` remains. The only hosted target is the AWS dev environment, which deploys from `main` on every push. Older notes below that mention Azure are history, not current instructions. The Microsoft (Azure AD) sign-in button is unrelated and unchanged.

**AWS dev is live and working — 17 September 2026:** The app auto-deploys to a cheap dev address on every push to main (ECR image plus App Runner, inside the $100 budget guard). Proven live in a real browser: account sign-up, organisation setup, dashboard with live counts, 2 risks, 2 linked tasks and an in-progress gap assessment with saved answers, all against your ComplianceHub Supabase project (wired off the old staging project, which is untouched; its 15 missing migrations were applied). CI is faster too: small changes clear in about a minute with the same four gates.

**Milestone 1 implementation plan drafted for review — 14 September 2026:** The approved GitHub organisation/repository design now has one phase-by-phase [implementation plan](superpowers/plans/2026-09-14-github-organisation-repository-integration.md). It reuses the existing secure GitHub foundation and sequences exact-permission/configuration hardening, complete paginated repository discovery, a separate durable connection-health state machine, fail-closed provider reconciliation, connection-only webhook/scheduled execution, deduplicated in-app/Slack incidents, Owner/Admin/Member interface correction, one shared web/runner image, the company AWS staging gate, and one-repository live acceptance. The plan deliberately keeps Monitoring compliance judgments, official Evidence/Findings/Tasks, general Platform Automation, full production AWS, wider rollout and MCP outside Milestone 1. This planning change does not implement application behavior, create AWS resources, configure a GitHub App or Slack destination, or prove a live connection. After plan approval, execution can begin with Phase 1; AWS deployment remains gated by the company AWS/security administrator's approved account, networking, IAM, secrets, logging, retention, cost, alert and delivery-path inputs.

**Milestone 1 GitHub integration design approved — 14 September 2026:** The Owner has approved the milestone-specific design for completing and hardening the existing company GitHub organisation/repository connection rather than rebuilding it. The design fixes Owner-only installation and repository-scope changes, Admin read-only health, Member exclusion, two-layer selected-repository scope, one pilot repository, exact read-only permissions (`metadata`, `administration`, `actions`, `vulnerability_alerts`, `security_events`, `secret_scanning_alerts`), minimal data retention, direct GitHub authorisation, automatic connection reconciliation, sanitised in-app/Slack incidents, separate staging/production Apps, and a minimum ECS Fargate/EventBridge/Secrets Manager/CloudWatch staging prerequisite. Compliance interpretation, additional permissions, the general built-in Automation engine, full AWS acceptance, production and wider rollout remain separate milestones or decisions. [Approved Milestone 1 design](superpowers/specs/2026-09-14-github-organisation-repository-integration-design.md). This is documentation only: no application code, AWS resource, GitHub App, provider connection, Slack destination or hosted release has been created or verified. The implementation plan is now linked above; infrastructure execution remains dependent on the company AWS administrator supplying the approved staging account, region, networking, IAM, logging, cost and retention inputs.

**Candidate 3 Workspace-access architecture complete for every safely migratable section — 14 September 2026:** The final Settings step now routes its exact Owner/Admin route and navigation, general team controls, Owner-only role/AI decisions, and invitation lifecycle controls through the organisations-domain Workspace-access policy. Invitation creation deliberately retains its existing target-role domain rule after rate limiting because adding a broad Settings gate would change its denial wording and order. Across the completed Candidate 3 programme, the shared module now answers route, navigation, presentation and operation access for all sections that had one internally consistent existing rule. No wording, validation, rate limit, query, mutation, redirect, navigation or visible behavior changed. Source `2ced09c` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh fictional Owner, Admin and Member sessions exercised 57 page loads and expected redirects on that exact build without changing business records. A fresh verified comparison against the preserved pre-Candidate-3 app at both desktop and the owner's 705-pixel width found zero changed pixels across all 84 screenshot pairs. Performance, the Automation inbox, Slack's Admin database access versus Owner-only application controls, and the existing Admin GitHub prompt versus Owner-only setup remain explicit product/security decisions; they were not disguised as architecture cleanup. This is local-only architecture work: hosted release, provider verification and human acceptance remain unfinished.

**Latest GitHub CI repair — 14 September 2026:** The dependency versions that caused the GitHub security-audit job to fail are patched without forcing the breaking ExcelJS downgrade, and the whole-product browser checks now follow the current onboarding, risk, audit, control-review, policy-template, evidence-source and Member read-only experiences. Three previously unreadable small labels use the existing accessible muted-text colour, and the demo's active navigation label now has 5.93:1 contrast. Source `7e2a844` is pushed to `codex/team-baseline`. [Fresh GitHub CI run 34808640025](https://github.com/mukta2701/ComplianceHub/actions/runs/34808640025) passed dependency installation, the high-severity security audit, lint, type checking, 3,146 unit checks with 3 intentional skips, a production build, 8 integration checks, the database upgrade rehearsal and database tests, secret scanning, the production-container build, and the complete browser run with 66 checks passing and 52 intentionally skipped; no browser retry was needed. The application change set is also healthy in a local production preview at `http://127.0.0.1:3301` against local Docker services. Two moderate ExcelJS/UUID advisories and GitHub Actions' Node 20 deprecation notices remain separate maintenance work. During diagnosis, an initially misdirected browser setup created nine synthetic hosted test workspaces; all nine login accounts were immediately soft-deleted, while their inert organisation rows remain because the database correctly blocks deleting a workspace's last owner. No hosted application release or live-provider behavior was changed or verified.

**Latest Workspace-access architecture step — Monitoring, 14 September 2026:** The Monitoring route, role-specific navigation and presentation, general-check permission, finding decisions, and official GitHub review/recheck/recovery controls now read one organisations-domain Workspace-access policy. Owners retain the full page and all controls; Admins retain the operator page and general checks without Owner-only finding/GitHub controls; Members retain their separate read-only page. The existing Admin connection prompt remains unchanged even though GitHub setup is Owner-only; resolving that product inconsistency is parked rather than silently changing behavior. No wording, validation, rate limit, query, mutation, redirect, navigation or visible behavior changed. Source `92929e8` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior; Admin behavior is freshly covered by the policy, page, action and component checks. This is local-only architecture work; the remaining safely migratable Settings boundary, the parked Performance and Automation-inbox contradictions, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Connections, 14 September 2026:** The private Connections route, Settings tab and breadcrumb metadata, page controls, integration actions, provider-connect role decisions and authorization-state role check now read one organisations-domain Workspace-access policy. Owners and Admins retain general connection management; GitHub App setup, Slack destinations and daily-digest channel selection remain Owner-only; Members remain redirected before reaching the page. Provider webhook/cron trust and callback cryptographic, state, identity, context and rate-limit checks remain outside workspace role policy and retain their existing order. No wording, validation, rate limit, query, mutation, redirect, navigation or visible behavior changed. Source `6d14dd4` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. A fresh 42-route visual comparison against the pre-Candidate-3 app at desktop and the owner's 705-pixel width found zero changed pixels across all 84 pairs. This is local-only architecture work; the remaining safely migratable Monitoring and Settings boundaries, the parked Performance and Automation-inbox contradictions, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Automation setup, 14 September 2026:** The `/app/setup` route's page presentation, owner-only save authorization and breadcrumb metadata now read one organisations-domain Workspace-access policy. Owners retain the setup form, Admins retain the same read-only explanation, and Members remain redirected before reaching the page. Automation inbox actions and scheduled cron authorization remain outside this setup-only policy because their existing role and database rules do not describe one consistent access boundary; they are parked for an explicit product decision rather than silently changed. No wording, validation, rate limit, query, mutation, redirect, navigation or visible behavior changed. Source `c53b7da` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior; Admin behavior is freshly covered by automated policy/page checks but was not re-demonstrated in the local app because the isolated database has no disposable Admin. This is local-only architecture work; the remaining safely migratable Candidate 3 sections, the parked Performance and Automation-inbox contradictions, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Tasks and member contributions, 14 September 2026:** Task routes, role-specific navigation metadata, page controls, operator mutations and contribution-review decisions now read one organisations-domain Workspace-access policy. Owners and Admins retain the full Work queue and management/review controls; Members retain the Assigned tasks shortcut, read-only list/detail presentation and live-assignee contribution workflow. Tracker delivery remains tied to connection-management authority. Existing direct gap-task, starter-calendar and export-handler seams retain their prior defenses without new early role denials. No wording, validation, rate limit, query, mutation, redirect or visible behavior changed. Source `c2a19c0` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. This is local-only architecture work; the remaining Candidate 3 sections, the parked Performance access contradiction, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Internal audits and activity, 14 September 2026:** Internal-audit routes, operator navigation metadata, page controls, Audit mutations and the separate activity route now read one organisations-domain Workspace-access policy. Owners and Admins retain the full audit register and management controls; Members remain redirected before reaching Internal audits or activity. Existing dormant Member audit rendering remains preserved but unreachable, while the public auditor page and audit-pack handler remain outside new local role checks. No wording, validation, rate limit, query, mutation, redirect or visible behavior changed. Source `a37b091` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Evidence, 14 September 2026:** Evidence route decisions, operator navigation metadata, page controls, Evidence mutations and export protection now read one organisations-domain Workspace-access policy. Owners and Admins retain the full vault and management controls; Members remain redirected before reaching the vault or export API. The existing dormant Member read-only rendering and file-download behavior were preserved without making them reachable. No wording, validation, rate limit, query, mutation, redirect or visible behavior changed. Source `080aab7` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Overview/dashboard, 14 September 2026:** The app-root route, role-specific navigation metadata and choice between the operational Dashboard and restricted Member Overview now read one organisations-domain Workspace-access policy. Owners and Admins retain the full Dashboard; Members retain their smaller read-only Overview and never load the operator queries. No wording, query, action, redirect or visible behavior changed. Source `a5772b4` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged separate experiences. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Notifications, 14 September 2026:** The Notifications route, header-bell metadata and breadcrumb title now read one organisations-domain Workspace-access policy. Owners, Admins and Members retain the same personal inbox and read controls; Notifications remains absent from the sidebar, and its own-row database protection is unchanged. No wording, query, mutation, redirect or visible behavior changed. Source `65b419e` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Trust Center, 14 September 2026:** The private Trust Center route, Owner/Admin management presentation and actions, and operator sidebar metadata now read one organisations-domain Workspace-access policy. Owners and Admins retain management, Members remain redirected, and the anonymous public Trust Center remains outside workspace membership checks. No wording, validation, rate limit, query, redirect or visible behavior changed. Source `e2de640` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Scope and context, 14 September 2026:** The Scope route, read-only/editor page presentation and owner-only save authorization now read one organisations-domain Workspace-access policy. Owners retain editing, Admins retain read-only access, and Members remain redirected from the route. No sidebar item, wording, validation, rate limit, query, redirect or visible behavior changed. Source `315c15e` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged reachable behavior. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Latest Workspace-access architecture step — Saved baseline, 13 September 2026:** Saved-baseline route decisions, read-only/operator page presentation and save authorization now read one organisations-domain Workspace-access policy. Owners and Admins retain the form and saving controls; Members retain the saved read-only explanation and history; the section remains absent from the sidebar. No wording, validation, rate limit, database query, redirect or visible behavior changed. Source `a3a6382` is pushed and its immutable production package is running independently at `http://127.0.0.1:3300` with matching application/database health. Fresh Owner and Member browser checks confirm the unchanged behavior. This is local-only architecture work; the remaining Candidate 3 sections, hosted release, provider verification and human acceptance remain unfinished.

**Approved post-architecture product direction — 13 September 2026:** After the current architecture and visual programme, the owner intends to follow five independently accepted milestones: company GitHub organisation/repository integration, trustworthy Monitoring, portable built-in Platform Automation, company-controlled AWS staging, and a company-wide ComplianceHub MCP assistant through company-owned Codex. The [product requirements](superpowers/specs/2026-09-13-trusted-compliance-platform-prd.md) define their shared outcome and boundaries; the [five-milestone roadmap](superpowers/specs/2026-09-13-trusted-compliance-platform-roadmap.md) defines the sequence and completion evidence. They are the sole active future product direction; the earlier top-level roadmap and older MCP planning/design documents have been removed from the active tree while remaining recoverable from Git history. Historical evidence remains for auditability but does not direct future work or prove current behaviour. These documents are planning artifacts, not proof that the milestones are implemented. Each milestone still requires its own reviewed design, phase-by-phase implementation plan and fresh acceptance. The release checklist remains the sole live status source.

**Approved AWS always-on automation design — 13 September 2026:** The owner has selected a company-controlled AWS environment as the future host for the ComplianceHub web application and built-in Platform Automation, while retaining Supabase temporarily. The approved design uses one immutable image for an ECS Fargate web service and a finite ECS Fargate automation runner started by EventBridge Scheduler. Every workspace receives internal recurring administration automatically; approved connections add provider work without a master Enable button or an Automation navigation section. PostgreSQL initially owns the durable schedule/job history, while contextual product pages and separate compliance/operations notification destinations expose meaningful outcomes and failures. [Approved design and verification boundaries](superpowers/specs/2026-09-13-aws-always-on-automation-design.md). This is documentation only: no AWS resource, runner, database migration, provider, Slack destination or hosted release has been created or verified, and this decision does not mark the separate Monitoring visual-design increment complete. The company AWS administrator must confirm the approved non-production account, region, networking, IAM, logging and cost controls before infrastructure work. After the Monitoring/GitHub increment is completed and verified, the next independent application milestone is the portable always-on automation foundation; AWS staging, live-provider proof, data-platform migration and production acceptance remain separate.

**Latest proof-to-audit Audit workspace refinement — 11 September 2026:** Internal audits now work as one connected operating workspace: plan the audit, review the checklist, link traceable proof, record findings, create corrective work and share a time-boxed read-only result. Checklist decisions, evidence freshness, finding status and task completion remain separate. Audit-only links now exclude company-wide readiness totals and use the audit's real framework; whole-workspace links use a neutral label. Full unit, lint, production-build, targeted database and connected browser checks pass. Desktop/tablet/phone accessibility and containment checks pass, and independent specification, standards and visual reviews have no material finding. [Before/after explanation, screenshots and verification](evidence/2026-09-11-proof-to-audit-audits.md). Application source `b82cfb3` is pushed and its immutable package is running at http://127.0.0.1:3300/app/audits with matching app/database health. This completes the Audits visual-design increment. Monitoring is the next visual-design increment; hosted release, live-provider verification, screen-reader acceptance and human stakeholder acceptance remain separate.

**Latest proof-to-audit Evidence vault refinement — 11 September 2026:** Evidence now works as a focused list-and-detail vault with truthful date-aware freshness, safe filters and pagination, complete source and linked-work context, and a responsive Evidence → Monitoring → Audits journey. Official GitHub evidence keeps its trusted provenance inside the normal evidence record; legacy records keep available references without pretending their collection identity is known. Adding evidence shows only the relevant required source field, stale replacement or maintenance requests fail safely, and withdrawing requires a deliberate second step while preserving history. Focused unit and database tests, type checking, lint, production build, the full connected browser workflow, and desktop/tablet/phone accessibility and containment checks pass. [Before/after explanation, screenshots and verification](evidence/2026-09-10-proof-to-audit-evidence-vault.md). Application source `37b86ec` is pushed and its immutable production package is running at http://127.0.0.1:3300/app/evidence with matching app/database health. This completes the Evidence-vault increment. Audits and Monitoring remain the next visual-design increments; hosted release, live-provider verification, screen-reader acceptance and human stakeholder acceptance remain separate.

**Latest expressive dashboard and shared visual foundation — 10 September 2026:** The programme dashboard now uses the approved stronger colour, hierarchy, graphical summaries and restrained hover/reveal motion while keeping every number tied to existing records. The saved-baseline route is explained as `Programme basis` beside the control position instead of competing in the page header. Shared navigation, buttons, cards, forms, tables, status labels and phone touch targets now use one semantic visual foundation; no unsupported trend, comparison, team chart or search was invented. Full unit, lint, type and production-build checks pass. Dashboard visual/accessibility/behavior checks pass at 1440px, 883px and 390px, the representative authenticated workspace suite passes, and independent specification/standards reviews have had their material findings resolved. [Before/after explanation, mock-up, screenshots and verification](evidence/2026-09-10-expressive-dashboard-visual-foundation.md). Application source `03bf777` is pushed, and its immutable production package is running at http://127.0.0.1:3300/app with matching app/database health. This completes the dashboard visual-foundation increment; page-specific refinement across the remaining product, hosted release, live-provider verification, screen-reader acceptance and human stakeholder acceptance remain separate.

**Latest connected Assessment → Controls → Statement of Applicability refinement — 10 September 2026:** Assessment now leads into a clearly named editable Control review and then an immutable formal Statement of Applicability. People can see the exact source assessment, mapped answers, accountable decisions, evidence freshness, linked work and truthful blockers without the application making compliance decisions for them. Repeated creation reuses one active review; database revisions prevent stale tabs or imports from overwriting newer work; essential read failures block finalisation. Members can follow authorised source links but cannot edit or finalise. Full unit, lint, type, build, integration and database checks pass, including a disposable migration-upgrade rehearsal. The affected production browser journey passes 14 of 14 checks across desktop and mobile, and independent review has no material finding. [Before/after explanation, architecture, screenshots and verification](evidence/2026-09-10-connected-controls-applicability.md). Application source `055b548` is pushed and its immutable package is running at http://127.0.0.1:3300/app/soa with matching app/database health. This completes the connected-controls increment. The older whole-product browser suite still has unrelated configuration and outdated-selector failures; hosted release, live-provider verification, screen-reader acceptance and human stakeholder acceptance remain separate.

**Latest policy-workspace refinement — 10 September 2026:** The Policy library, document detail, create/edit, approval and employee-acceptance paths now work as one responsive journey. Coordinators see accurate ownership, lifecycle, version, review and acceptance context; Members read the document before acceptance and only receive Evidence links they are authorised to open. Recoverable forms preserve drafts, templates ask before replacing work, and database-backed revision checks prevent stale edits, approvals or acceptances from changing records. Full unit/lint/type/build, integration/database and final production desktop/mobile browser checks pass. Independent specification, standards and visual reviews have no material remaining finding. [Before/after explanation, screenshots and verification](evidence/2026-09-10-policy-workspace-refinement.md). Application source `dd84665` is pushed and its immutable package is running at http://127.0.0.1:3300/app/policies with matching app/database health. This completes the policy-workspace increment; hosted release, live-provider verification, screen-reader acceptance and human stakeholder acceptance remain separate.

**Latest asset-workspace refinement — 10 September 2026:** The Asset inventory, detail, create/edit, linked-risk, import and export paths now work as one responsive, reliable journey. Exact summary counts and explicit display caps keep large inventories honest; accountable in-app ownership remains separate from descriptive location; recorded controls remain separate from reviewed evidence. Recoverable forms preserve drafts and reject stale overwrites, including automatic owner/category removal. Import category/reference handling and complete CSV/XLSX pagination now fail safely instead of reporting partial success. Full unit/lint/type/build, integration/database, production desktop/mobile, Member-access and accessibility checks pass. Independent specification, standards and visual reviews have no material remaining finding. [Before/after explanation, screenshots and verification](evidence/2026-09-10-asset-workspace-refinement.md). Application source `3a050a0` is pushed and its immutable package is running at http://127.0.0.1:3300/app/assets with matching app/database health. This completes the asset-workspace increment; hosted release, live-provider verification and human stakeholder acceptance remain separate.

**Latest risk-workspace refinement — 10 September 2026:** The Risk register, detail and create/edit routes now work as one responsive operating journey. Current exposure, appetite configuration, overdue reviews and missing owners lead the register; desktop/tablet/phone records retain the same scores, ownership, review, linked work, evidence freshness and authorised actions. Risk detail connects source tasks, managed evidence and assets, while cancelled treatment work stays distinct from completion. Recoverable forms retain drafts and reject stale overwrites. Current assessment gaps are verified against their workspace source before becoming risks. Full unit/type/lint/build, integration/database, production desktop/mobile browser and accessibility checks pass. Independent standards, specification and visual reviews have no material remaining finding. [Before/after explanation, screenshots and verification](evidence/2026-09-10-risk-workspace-refinement.md). Application source `cabc4ff` is pushed and its immutable package is running at http://127.0.0.1:3300/app/risks with matching app/database health. This completes the risk-workspace increment; hosted release, provider verification and human stakeholder acceptance remain separate.

**Latest task-form refinement — 10 September 2026:** New and Edit Task now follow the approved mockup with clear task-brief, ownership/timing and linked-record sections, explicit Back/Cancel actions, visible required/optional cues and responsive controls. Recoverable validation/write errors preserve the operator's entries; stale tabs cannot overwrite newer changes; and saved owners, controls and risks remain available even when omitted from a large choice page. Full unit/type/lint/build, integration/database, desktop/mobile browser and accessibility checks pass. Independent specification, standards and visual reviews have no material remaining finding. [Before/after explanation, screenshots and verification](evidence/2026-09-10-task-form-refinement.md). Source `8c5b729` is pushed and its independent production package is running at http://127.0.0.1:3300/app/tasks/new with matching app/database health. This completes the task-form increment; it does not establish hosted release or human stakeholder acceptance.

**Latest accountable-work redesign — 10 September 2026:** The approved visual direction is now applied to the Tasks register and evidence submission/review journey. The work queue combines exact attention counts, dated deadline health, the operator's review queue and responsive task rows. Task detail leads with assignment, submission and independent decision, keeps task completion separate from evidence acceptance, and supports multiple tracker links. Full unit/database/type/lint/build checks, desktop/mobile fictional workflows and independent specification, standards and visual reviews pass. [Before/after and verification](evidence/2026-09-10-task-work-review-ui.md). Source `a9dbe88` is pushed and its independent production package is running at http://127.0.0.1:3300/app/tasks with matching app/database health; the affected browser journeys also pass against this background process.

**Latest dashboard redesign — 9 September 2026:** The approved visual direction is now implemented in the dashboard and shared navigation. Programme overview adds real attention counts, clearer control bars, a compact decision queue, risk/evidence graphics, upcoming work and workspace breadcrumbs. Full and affected checks, independent reviews and production desktop/tablet/mobile demonstrations passed. Source is pushed to GitHub and the refreshed app runs independently at http://127.0.0.1:3300/app with matching app/database health. Existing baseline records are preserved. [Before/after and verification](evidence/2026-09-09-programme-dashboard.md). Remaining page-specific mockups, live-provider work and hosted/human acceptance are unfinished; this is the first implemented redesign increment.

**Visual proposal — 9 September 2026:** A [product-wide mockup set](design/2026-09-09-product-mockups/README.md) now illustrates 18 desktop/mobile views across the ongoing lifecycle, with Mobbin, Linear and SaaSFrame references. These are proposed layouts with fictional data, generated and visually inspected; they are not implemented screens or new acceptance evidence. The notes identify generated details that must not become product rules.

Updated 10 September 2026. Historical release evidence below is retained. Fresh checks today confirm the accountable-work candidate and preserved local database. The connected owner-submission, coordinator-review and saved-leadership-baseline workflow has been demonstrated locally. This is fictional local proof, not hosted or stakeholder acceptance.

**Latest recovery and quality batch — 9 September 2026:** The updated app runs independently in the background as a **production preview at http://127.0.0.1:3300/app**, against preserved fictional local data. App/database health, authenticated policy access and restored live-update subscriptions pass. About 12 GiB disk space remains after clearing only regenerable build caches. The low-swap diagnostics implicate a roughly 12 GiB Node process on this 16 GiB Mac; resource protection reduces that risk without proving all shutdown causes resolved. [Recovery evidence](evidence/2026-09-09-mac-recovery.md).

The first company-wide quality batch is **implemented, independently reviewed and verified locally**: policy accountability/recoverable saves, repeat review cycles, connected risk/asset context, truthful control guidance and collection failure visibility. Full unit/database checks pass; all material review findings and initial production browser failures have passing focused reruns. Final desktop/mobile visual review is clear. Verified application source and fictional screenshots are pushed to the existing GitHub feature branch; the background preview identifies that source version. [Changes, verification and screenshots](evidence/2026-09-09-operating-lifecycle-quality.md). Hosted release, live-provider verification and stakeholder acceptance remain separate unfinished gates.

**Earlier recovery snapshot — 9 September 2026, 18:08 BST:** The owner reported a laptop shutdown during ongoing quality work. The latest development app is restored at `http://127.0.0.1:3300/app`. Colima initially failed to start because its retained disk was marked in use; stopping its stale runtime state and restarting restored the existing Docker/database services. No database reset or record deletion was performed. Fresh recovery evidence: HTTP health returns `status: ok, db: ok`; the existing fictional coordinator signs in and opens the saved policy, and its assigned owner is retained. This identifies the restart blocker, not the cause of the laptop shutdown. Interrupted browser runs are incomplete and must resume before the feature batch is declared verified. New application source remains work in progress; only the skill setup was already pushed before shutdown. The earlier 3100/3200 server processes have not been relaunched in this recovery; use 3300 for the current working app.

**Overall:** The fictional local showcase has been demonstrated and packaged. The latest release has not been accepted on company staging. The full live GitHub fix-and-recheck journey remains unproven in this showcase.

| Area | Status | What this means for you |
|---|---|---|
| Core compliance showcase | Demonstrated locally | Saved browser checks cover assessment, control decisions, risk, task, evidence, audit and report journeys using fictional Northstar data. |
| Assigned work and coordinator review | Fresh local production demonstration | Assigned Members submit notes; a separate coordinator requests changes or accepts linked evidence. Desktop/mobile and concurrent API checks pass with fictional data. [Change evidence](evidence/2026-09-09-team-contributions.md). |
| Saved baseline and leadership explanation | Fresh local production demonstration | The coordinator resumes progress and saves dated source records; Members read preserved history, limitations and comparable changes. [Change evidence and architecture](evidence/2026-09-09-team-baseline.md). |
| Human review of an issue | Demonstrated locally | A separate fictional follow-up shows a task completed, evidence reviewed and an audit finding explicitly closed. Task completion alone does not prove resolution. |
| GitHub checks and AI read access | Earlier live proof; current use needs verification | Earlier collection and seven MCP read/preparation tools were demonstrated. The current showcase launcher disables provider credentials; saved results do not prove a working live connection today. |
| Broader company-wide AI assistant | Planned beyond the evidenced tool set | The approved design describes more coverage than has been demonstrated. In-app AI drafting is a separate feature whose current configuration and answer quality have not been established in this review. |
| Latest company staging release | Blocked / not accepted | Latest recorded Azure target access failed. Access, deployment approval and hosted verification remain outstanding. |
| Reliability and first-user acceptance | Open | Recovery from a previous app stall is documented; its original cause remains uncertain. Independent acceptance by the intended first user has not been established. |

### Current focus and finish line

**Workspace UI — verified locally, pushed and running.** The owner requested a consistent design using Mobbin references. Shared navigation, headings, controls and tables are reorganised; Settings now shows one section at a time with readable member rows and authorised editing. Full unit/type/lint/build, independent standards/specification/visual review and four production browser checks pass. Saved editing, history, desktop/tablet/mobile layouts, keyboard navigation and accessibility scans are demonstrated with fictional data. [Before/after evidence](evidence/2026-09-09-workspace-ui.md). Application source `d5cd2f1` is pushed and running independently at 3300; fresh health confirms app/database access and the exact source. Both actual-background browser journeys pass, and the user’s in-app browser shows the new Settings screen with preserved fictional team records. Hosted and human acceptance remain open.

**Dated collector observations — verified locally, pushed and running.** The owner approved keeping a dated snapshot even when facts are unchanged. [Specification](superpowers/specs/2026-09-09-dated-collector-observations.md) and [ticket10](plans/operating-lifecycle-quality/issues/10-dated-collector-observations.md) preserve previous evidence/review decisions, deduplicate identical retries and retain current manual versus scheduled collection scope. Implemented from `398e01d`; full unit/database checks, local collection recovery, independent reviews and final desktop/mobile production demonstrations pass. [Evidence and screenshots](evidence/2026-09-09-dated-observations.md). Source `9b831c5` is pushed and running independently at 3300. Fresh app/database health identifies that source; the fictional authenticated journey passes on the actual background process too. The older local migration-ledger mismatch is now repaired after confirming its complete schema already exists. Normal local upgrades succeed without applying anything; all 95 public-table fingerprints and the schema are unchanged. [Local maintenance evidence](evidence/2026-09-09-local-migration-history.md). No database reset was used.

**Explicit asset-owner increment — verified locally and running:** Owner approved a separate In-app owner import column. Location remains descriptive; invalid or ambiguous named owners will be flagged rather than assigned. [Specification](superpowers/specs/2026-09-09-explicit-asset-import-owner.md). Action/adapter, full unit/type/lint/build and local asset database checks pass; desktop/mobile imports demonstrate correct saved assignments and excluded unsafe names. [Evidence](evidence/2026-09-09-explicit-asset-owner.md). Source `e35112e` is committed/pushed and included in the newer dated-observation build running at 3300; its owner guidance was verified in the actual browser.

**Import usability increment — verified locally, pushed and running:** Changed mappings, target registers or workbooks now require a fresh preview; pending inputs stay stable and confirmation consumes the preview. Phone mapping controls now fit the screen. Full automated checks, final affected checks and both desktop/mobile production-browser journeys pass. Application source `165f975` is pushed to the existing GitHub feature branch and running independently at 3300; fresh health confirms the database and exact source identity. [Evidence and screenshots](evidence/2026-09-09-import-preview.md). [Specification](superpowers/specs/2026-09-09-import-preview-consistency.md). Separately, asset ownership inference conflicts with an older design decision; the owner has now approved a separate explicit column, implemented in the next bounded increment.

**Follow-on availability batch:** Dashboard and audit query failures are now visibly unavailable rather than reassuring empty or zero results. The page retry action now fetches recovered server data. Full unit/type/lint/build checks and fictional production desktop/mobile failure/recovery demonstrations pass; independent standards/specification and visual reviews found no material issues. [Verification and screenshots](evidence/2026-09-09-data-availability.md). Verified source is pushed to GitHub and now runs independently in the background at http://127.0.0.1:3300/app. Fresh health identifies the demonstrated source; the actual refreshed dashboard and audit register are usable.

**Latest direction — ongoing company-wide quality programme (9 September 2026):** Improve the full security and compliance operating lifecycle, not just onboarding or one coordinator/leadership example. The [current assessment and linked increments](plans/operating-lifecycle-quality/assessment.md) distinguish existing capabilities, confirmed defects, proposed additions and deferred decisions. The first local increment now covers policy accountability and save feedback, repeat review cycles, connected asset/risk context, truthful control guidance and collection failure visibility. Its integrated checks, independent review and fictional production demonstration are recorded above. Repeated collector observation semantics and broader acceptance remain deferred as recorded in the assessment. Matt Pocock's eleven requested skills are installed at repository scope, with existing local tracking and domain-document conventions retained. This supersedes earlier text that treated the initial Mukta/Charlie baseline scenario as the limit of the product. It does not authorise hosted deployment or establish human/provider acceptance.

**Earlier baseline milestone and company-specific context (retained history):**

Current implementation focus, approved 9 September 2026: build the first connected workflow from **ComplianceHub's product vision and backend operating workflow** for a software startup with at least around 20 employees, with Mukta coordinating the programme, accountable owners contributing their work, and Charlie reviewing the baseline. Assigned contribution, coordinator review, resumable baseline and dated leadership explanation are implemented, reviewed and demonstrated in the separate local production preview. Source is pushed to GitHub. The familiar demo retains its earlier application version; the new workflow is available in the separate preview. The [product and architecture draft](superpowers/specs/2026-09-09-compliancehub-focused-architecture-design.md) explains how existing capabilities should reduce manual administration and support evidence-linked leadership decisions. The owner authorised implementation after reviewing the plan. A separate local test database and fictional accounts isolate the new work from the existing demo. The mAIself staging/Gmail security trace remains deferred by the owner. The longer-term purpose remains establishing a security baseline for **mAIcompany's business and its mAIself product** using the existing ComplianceHub capabilities. Owner-confirmed map: `mAIself` contains the backend; `maiself-web-next` is upcoming design-system/web work; `grove-maiself` is not used. This supersedes earlier company-name and active-Grove assumptions. The owner reports Charlie's backing, internal testing with real Gmail/calendar connections, and Charlie/Aman (CTO) as technical confirmation contacts. No customer users are reported. This does not establish that existing engineering safeguards are absent.

The [draft system inventory](maiself-system-inventory.md) includes read-only observations from the explicit AdTecher Azure subscription, the deployed staging frontend and public mAIself health endpoints, in addition to version-pinned repository sources. The mAIself staging/Gmail trace is now deferred by the owner. The current focus is ComplianceHub itself: define its operating workflow, then use it to establish the first security baseline. No application changes were made during the earlier architecture planning. The first connected journey under the [team-baseline implementation plan](superpowers/plans/2026-09-09-team-baseline.md) is demonstrated with fictional accounts: owner submission, change request/resubmission, independent coordinator acceptance, saved partial baseline, preserved earlier versions and leadership explanation. All eight fresh production desktop/mobile UI and API checks pass on the final build, including closed-task review guidance and reopening; all 2,154 database checks pass. The full unit run retains one pre-existing SoA colour-style assertion failure alongside 2,544 passes and three skips. During final validation the local database environment exhausted its 2 GB memory allowance; increasing it to 4 GB and restarting the retained services restored healthy operation. Both local apps are running again. This does not explain the older app stall. Next checkpoint: Mukta and Charlie review the first intended-team baseline and measure whether it reduces manual chasing and report preparation. No external messages or hosted deployment have been performed.

The technical fictional-demo milestone is evidenced as complete. The mentoring milestone is complete when the owner can explain that journey and its limitations. This does not close live GitHub, staging or customer-acceptance work.

The first stakeholder is Charlie, for internal mAIcompany business and mAIself product security work. The intended initial direction is ISO 27001 readiness; a release deadline and formal certification commitment remain unconfirmed. Other organisations are a possible later audience. Do not infer a commitment to external launch from completion of the local demo.

Role clarification confirmed by the owner on 9 September 2026: **Mukta operates ComplianceHub; Charlie reviews the baseline.** The architecture draft uses this division for the first-use journey. This documentation update does not change application permissions or establish fresh runtime, provider or stakeholder acceptance evidence.

### How we keep the biggest risks manageable

| Risk | Working rule | What remains unresolved |
|---|---|---|
| Work keeps expanding | State one bounded outcome before implementation; park unrelated ideas in existing planning notes. | Charlie is the first stakeholder. Confirm the real testing environment and baseline before choosing further feature work. |
| Completion reports are confusing | Report changed, verified, unfinished and next; always name local/demo/live environment. | Apply this consistently after each work session. |
| Status overstates proof | Keep task completion, human review, evidence freshness and fresh provider verification separate. | Live fix-and-recheck rehearsal is outstanding. |
| Evidence is confused with proof | ComplianceHub may store evidence, its source, date, owner and review decision, but it must not imply that a check happened when no evidence has been supplied or verified. A task marked Done is not proof by itself. | For each control, obtain the actual record and record the human review; until then, keep the status unverified or unknown. |
| Wrong version or stale proof | Tie demonstrated behavior to its saved release and environment; mark historical checks explicitly. | A saved passing check is not a fresh health check. |
| External access blocks release | Name the dependency and who must act; continue only independent in-scope work. | Owner/company must resolve Azure access; deployment and live repository scope remain separate decisions. |
| Product may not fit the first user | Ask the intended user to try the agreed journey and record where they get stuck. | No independent first-user acceptance established. |

### Evidence behind this summary

- Latest packaged release: `27ba366`; functional browser candidate: `16c25f2`. The package records documentation-only differences between them.
- Saved acceptance: eight desktop/mobile browser checks passed; packaged release sign-in, restart and refresh recorded on 6 September 2026.
- [Operator guide and demonstration](demo-release.md).
- [Private local release identity](../artifacts/human-review-2026-09-06/release-package.json), available with this local workspace; not a public artifact.
- [Earlier GitHub and MCP proof](evidence/phase3-direct-mcp-live-2026-09-04.md).

## Engineering history — dated checkpoints below

### Shared Workspace access — Settings and final safe-boundary audit — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact `/app/settings` route, Owner/Admin Admin-group navigation metadata, general team-management presentation, Owner-only member-role and AI-setting decisions, and invitation revoke/resend permission. Owners retain all Settings controls. Admins retain Settings and ordinary-Member management/invitation controls without role promotion or AI controls. Members remain redirected before Settings loads and have no Settings navigation. Personal OAuth grants and workspace creation/switching remain outside workspace-role policy. New invitation creation deliberately keeps its existing target-role domain authorization after the existing rate limit: a broad Settings gate would change both the established order and `Your role cannot invite that role` denial. Every visible word, validation rule, rate limit, query, mutation, redirect, navigation and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Test-first source verification: five Settings ownership/contract cases failed while the section and scattered checks were absent; the first focused access suite passed 92/92, then the broader unchanged Settings, team, invitation, OAuth, portal and navigation suite passed 228/228. Independent read-only review found no access, message, query, mutation, rate-limit, navigation or title drift and identified the invitation-creation exception, which is now explicit in the contract. The final ordered gate passes on the isolated intended patch: typecheck, lint, the complete unit suite with 351 files and 3,146 tests passed plus three intentional skips, the required workspace-UI and team-contribution browser suite 8/8 across Chromium desktop/mobile, and the Next.js 16.3.5 production build. Existing hydration, local-storage and transient connection-reset warnings did not fail the suites. These checks prove source and test environments, not the independently running app.
- Application source `2ced09c70195271074c54c4bf93de2ffe2b3e592` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in page loads. Fresh Owner and Admin sessions each loaded 19 representative application sections; the Owner retained Admin invitation and AI controls, while the Admin retained ordinary invitation management without those Owner-only controls. A fresh Member session loaded all ten expected Member-visible sections, was redirected from all nine tested operator-only sections, showed `Member view`, and had no Settings navigation. No form was submitted and no business record changed; only a retained fictional Admin account's local authentication password was reset for this check. Stale already-open browser tabs requested obsolete Server Action identifiers after application replacement, but fresh sessions completed successfully and health remained good.
- A final verified differential audit ran the preserved pre-Candidate-3 production package and source `2ced09c` against the same current fictional database and Owner account. It captured 42 authenticated route scenarios at both 1,440-pixel desktop and the owner's 705-pixel browser width. All 84 screenshot pairs were byte-for-byte identical: zero changed pixels, zero size differences and zero route-outcome differences. This proves Candidate 3 did not change those rendered layouts; it does not prove mutations, hosted behavior or live-provider behavior.
- The final source audit found no missed section with one consistent existing rule. Remaining raw role decisions are either display labels, the separate MCP membership/data-minimisation boundary, target/assignment-specific domain authorization, or known contradictions. Performance contains a Member presentation that the portal currently makes unreachable. Automation inbox mixes an operator-only route, Owner-only connection/baseline controls and assignment-scoped proposal actions. Slack database policy permits Admin access while application controls are Owner-only, and Monitoring still prompts Admins toward Owner-only GitHub setup. Changing any of these would widen or narrow access, so they remain named product/security decisions for separate work. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Monitoring — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact `/app/monitoring` route, Owner/Admin and Member navigation metadata, operator-versus-Member page presentation, general monitoring-run permission, legacy finding controls, and every official GitHub finding/recheck/mapping/processing/recovery decision. Owner-only official GitHub controls use a dedicated capability so a later change to ordinary finding management cannot accidentally widen provider-control authority. Owners retain the full page and all controls. Admins retain the operator query/presentation and general-check authority but not Owner-only finding or official-GitHub controls. Members retain the separate safe read-only loader/page and Monitoring navigation. Alert polling remains outside Monitoring management, and provider webhooks/cron trust remains outside workspace membership policy. The existing Admin `Connect GitHub`/`Choose repositories` prompts still use general connection authority and remain visible exactly as before even though GitHub App setup is Owner-only. Every visible word, result shape, redirect, validation rule, rate limit, database call, query shape, mutation and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Test-first source verification on `codex/team-baseline`: eight access-contract/source-ownership cases failed while Monitoring was absent and its decisions remained local. The focused Monitoring/access/page/action/component suite then passed with 18 files and 280 tests. Independent read-only review found no current access, ordering, query, message or render drift; its future-coupling concern was resolved with the dedicated Owner-only official-GitHub capability, after which the focused safety suite passed with nine files and 167 tests. The final ordered gate passes: typecheck, lint, the complete unit suite with 350 files and 3,139 tests passed plus three intentional skips, the required workspace-UI and team-contribution browser suite 8/8 across Chromium desktop/mobile, and the Next.js 16.3.5 production build. Existing hydration, local-storage and transient connection-reset warnings did not fail the suites. These checks prove source and test environments, not the independently running app.
- Application source `92929e8b6fdcb5a0b2868c8a2d518cce29a29bc0` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA; the unauthenticated sign-in route returns HTTP 200 and the log remained stable across the check. An authenticated Owner loads the unchanged Monitoring breadcrumb, active-findings area and connection-management link. A separately signed-in fictional Member loads `/app/monitoring`, sees `Member view`, the exact read-only explanation and Monitoring navigation, and sees no connection-management link. No form was submitted and no business record changed. Admin behavior is fresh in the policy/page/action/component checks but was not yet demonstrated in this retained local workspace. No database schema, staging, hosted release, live-provider or acceptance gate changed.
- The Admin connection-prompt versus Owner-only GitHub setup inconsistency remains an explicit product decision, not an architecture defect concealed by this migration. Performance and Automation-inbox access contradictions also remain parked. Settings is the final safely migratable route boundary identified by the current audit.

### Shared Workspace access — Connections — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact private `/app/integrations` route, its Settings tab and breadcrumb metadata, Owner/Admin general connection-management permission, and the three existing Owner-only operation boundaries for GitHub App setup, Slack destinations and daily-digest channel selection. The page, integration actions, GitHub and Jira connect/callback role decisions, and consumed authorization-state role check now read that shared contract. Provider webhooks and scheduled routes remain outside workspace membership policy. Callback cryptographic/state/identity/context checks and their ordering remain intact. Every visible word, thrown error, redirect, validation rule, rate limit, database call, query shape, mutation and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: 17 access-contract/source-ownership cases failed first while Connections was absent and its decisions were scattered; the final focused access, page, action, route and authorization-state suite passes with 18 files and 344 tests. Independent read-only reviews found no access, ordering, query or visible-behavior drift. Typecheck and lint pass; the complete unit suite passes with 349 files, 3,131 tests passed and three intentional skips. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. The production build passes with Next.js 16.3.5. Existing hydration and transient connection-reset warnings did not fail the suite. These checks prove source and test environments, not the independently running app.
- Application source `6d14dd4706fe1cb3cbe63d0989b4a9de3f8c4b4e` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route loads. An authenticated Owner sees the unchanged Connections page, three provider cards and setup controls. A separately signed-in fictional Member is redirected from `/app/integrations` to `/app` and sees no Connections navigation. The two validated disposable accounts' local authentication passwords were reset for this check; no business record changed. Stale browser tabs briefly requested old Server Action identifiers immediately after application replacement; those requests stopped after refresh and health remained good.
- A fresh differential browser audit then ran the pre-Candidate-3 application and source `6d14dd4` against the same current fictional database and account. It captured 42 authenticated route scenarios at both 1,440-pixel desktop and the owner's 705-pixel browser width. All 84 screenshot pairs were byte-for-byte identical: zero changed pixels, zero size differences and zero route-outcome differences. This proves the architecture work has not changed those rendered layouts; it does not prove action authorization or hosted behavior.
- Two pre-existing product/security inconsistencies were deliberately not changed. The application treats Slack destination administration as Owner-only while the existing `alert_channels` database policy/grants permit Admin direct Data API access, including encrypted configuration. Monitoring also offers Admins a `Connect GitHub` prompt even though GitHub App setup is Owner-only. Both require an explicit product/security decision in their own bounded work; silently reconciling either here would change behavior. Performance and Automation-inbox contradictions likewise remain parked. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Automation setup — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact `/app/setup` route decision, Owner/Admin page presentation, owner-only setup mutation and breadcrumb metadata. Owners retain the same setup form and save control. Admins retain the same read-only explanation. Members remain redirected by the existing portal restriction before the page loads. The section remains absent from navigation. The owner-denial wording remains exactly `Only workspace owners can set up automation`, and its placement before rate limiting, validation and database work is unchanged. Automation inbox actions and all cron routes intentionally remain outside this setup policy: the inbox is route-blocked for Members while some assignment-scoped review operations and the current task-draft database path admit broader authority, so claiming one role policy would conceal a real contradiction or change behavior. No product behavior, visible word, validation, rate limit, query, mutation, redirect or existing test was deleted or changed.
- Fresh source verification on `codex/team-baseline`: the Automation setup access contract and source-ownership tests failed first while the policy was absent and role comparisons remained local; the final focused access/setup suite passes with 4 files and 154 tests. Independent read-only review found no material behavior, ordering, query, navigation or wording drift after the inbox was removed from this migration. Typecheck and lint pass; the complete unit suite passes with 348 files, 3,106 tests passed and three intentional skips. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. The production build passes, including a rebuild after the source commit so the runtime package carries the exact committed identity. Existing hydration and transient connection-reset warnings did not fail the required browser suite. A separate attempt to run the broader Automation browser journey did not pass because the local proposals query expects `source_objects.observation_key` while that column is absent even though migration history records its migration as applied; no schema or persisted-data repair was authorised or attempted in this relocation step. These checks prove source and test environments, not the independently running app.
- Application source `c53b7dab4a604fab6e93a0e8c5b3c950df5f964e` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner opens `/app/setup` and sees the unchanged source choices, review-owner selectors and `Save setup and open Automation` control; no setup was submitted. A separately signed-in fictional Member is redirected from `/app/setup` to `/app`. The disposable accounts' local authentication passwords were reset for this check; no business record changed. The in-app browser was refreshed onto this exact build and shows the same Owner setup page. The database contains no disposable Admin, so Admin read-only behavior is freshly verified by the policy/page tests rather than claimed as a live-app demonstration. No database schema, staging, hosted release, live-provider or acceptance gate changed.
- A separate fresh visual-regression audit compared the pre-Candidate-3 build `16b9328` with the then-running Candidate-3 Tasks build `c2a19c0` across 42 authenticated route scenarios at both 1,440-pixel desktop and the owner's 705-pixel browser width: all 84 screenshot pairs, route outcomes and statuses matched exactly after waiting for client-loaded Monitoring content. This proves the stacked Tasks cards and hamburger navigation shown at 705 pixels pre-date Candidate 3; it does not substitute for the newer setup source/runtime checks above.

### Shared Workspace access — Tasks and member contributions — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact task-list, UUID-detail, new-task, UUID-edit, gap-task and task-export route decisions; Owner/Admin and Member navigation metadata; page management presentation; operator task mutations; contribution-review permission; and tracker-delivery permission. Owners and Admins retain the same full Work queue and controls. Members retain the same `Assigned tasks` shortcut, tenant task-list/detail access and contextual contribution form when they are the live assignee; the transactional database function still independently checks assignment, revision, task state and membership. Tracker delivery remains specifically tied to connection-management authority through an operation-level Tasks policy. Existing gap-task creation, starter-calendar creation and export handling retain their prior defense boundaries without new early action denials. Every visible word, error, redirect, validation rule, rate limit, database call, query shape, mutation and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: 15 access-contract and source-ownership cases failed first while Tasks was absent and its decisions were scattered; the final focused Tasks/access/navigation/proxy suite passed with 23 files and 298 tests. Independent read-only review found and then confirmed the correction of tracker capability ownership, with no remaining material behavior drift. Typecheck and lint pass; the complete unit suite passes with 347 files, 3,094 tests passed and three intentional skips. One unrelated policy-form timing case failed during an earlier invalid run, then passed immediately alone and in the complete fresh rerun. The production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. Existing hydration warnings did not fail the suite. These checks prove source and test environments, not the independently running app.
- Application source `c2a19c03cd9a7cefde0c2d6ad3b51b73d345aec0` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in page loads. An authenticated Owner sees the unchanged Work queue, task-management controls, pending-review queue and task-detail review/status controls. A separately signed-in fictional assigned Member sees the unchanged `Assigned tasks` navigation, `Tasks` breadcrumb, task detail and `Submit for review` control; sees no Edit task or task-status form; is redirected from `/app/tasks/new` to `/app`; and receives the unchanged HTTP 403 `Workspace operator access required` response from the task export API. The disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Internal audits and activity — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact Internal-audit register, detail and new-audit route decisions, audit-pack and auditor-link API route decisions, Owner/Admin Oversight navigation metadata, page controls and Audit mutation authorization. Owners and Admins retain the same full register and management controls; Members remain redirected before reaching Internal audits or activity. The dormant Member audit presentation remains intact but unreachable. The existing audit-pack handler received no new local action gate, and the public `/audit-view/[token]` journey remains outside workspace membership policy. Both operator-denial messages retain their exact wording. Every other visible word, error, redirect, validation rule, rate limit, database call, query shape, mutation and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: 17 access-contract and source-ownership cases failed first while Audit access was absent and its decisions were scattered; the final focused Audit/access/navigation/proxy suite passed with 20 files and 212 tests. Independent read-only review found no material access, presentation, query or action drift. Typecheck and lint pass; the complete unit suite passes with 346 files, 3,076 tests passed and three intentional skips; the production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. Existing hydration and connection-reset warnings did not fail the suite. These checks prove source and test environments, not the independently running app.
- Application source `a37b091e7e621fa6243925309281a097662e6221` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged Internal-audit register and detail-management controls. A separately signed-in fictional Member is redirected from the register, detail, new-audit and activity routes to `/app`, and receives the unchanged HTTP 403 `Workspace operator access required` response from both protected Audit APIs. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Evidence — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact Evidence-vault, new-Evidence and export route decisions, Owner/Admin Work-navigation metadata, page controls and mutation authorization. Owners and Admins retain the same full vault and management controls; Members remain redirected before reaching the vault, new page or export API. The existing dormant Member read-only page branch and ungated file-download action remain intact and were not made reachable. Every visible word, error, redirect, validation rule, rate limit, database call, query shape, upload, cache invalidation and download behavior is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: eight access-contract and source-ownership cases failed first while Evidence was absent and its decisions were scattered; the final focused Evidence/access/navigation/proxy suite passed with 14 files and 167 tests. Independent read-only review found no behavior drift and identified the proxy-level export assertion added before the final gate. Typecheck and lint pass; the complete unit suite passes with 345 files, 3,052 tests passed and three intentional skips; the production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. Existing hydration warnings did not fail the suite. These checks prove source and test environments, not the independently running app.
- Application source `080aab7218a4fb1c7735704c4a44b3b1175b552b` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged `Evidence vault`, export links, `Add evidence` control and record-management controls. A separately signed-in fictional Member is redirected from `/app/evidence` to `/app`, sees no vault content, and receives the unchanged HTTP 403 `Workspace operator access required` response from the Evidence export API. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Overview and dashboard — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module now owns the exact `/app` route and the existing role-specific presentation decision. Owners and Admins retain `Dashboard` navigation/title and the full operational page; Members retain `Overview` navigation/title and the restricted Member loader/component. The manual Member-root route and both duplicated navigation tuples were removed only after their exact replacement was active. The existing Member short-circuit still prevents operator dashboard queries. No dashboard query, calculation, link, visible word or task action changed, and no new authorization was added to the starter-calendar action. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: seven access-contract and source-ownership cases failed first while Overview was absent and its decisions were scattered; the focused dashboard/access/navigation suite then passed with 9 files and 166 tests. Independent read-only review found no material access, presentation, query or action regression and confirmed the shared role-presentation interface removes duplicated role interpretation. Typecheck and lint pass; the complete unit suite passes with 344 files, 3,040 tests passed and three intentional skips; the production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. Existing hydration and cancelled-request warnings did not fail the suite. These checks prove source and test environments, not the independently running app.
- Application source `a5772b44bac512bff316845d92fdc3b3dee0ac23` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged `Dashboard` breadcrumb and `Programme overview` operational page. A separately signed-in fictional Member sees the unchanged `Overview` breadcrumb and `Welcome to …` restricted page, with no `Programme overview` content. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Notifications — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module is now the single source for the exact Notifications route and the header bell's href, label, icon and breadcrumb title. Owners, Admins and Members retain the same personal inbox and read controls; Notifications remains absent from the sidebar. The manual Member-route entry was removed only after its exact replacement was active. Notification actions intentionally retain only their existing application-context gate: no new role denial was added, and database RLS continues to limit reads and updates to the signed-in user's own rows. Every visible word, error, redirect, query, mutation and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: six access-contract and source-ownership cases failed first while Notifications was absent and its metadata was scattered; the focused Notifications/access/layout/navigation suite then passed with 7 files and 119 tests. Independent read-only review found no access or visible-behavior regression; its design concern about describing a personal self-update as workspace management was resolved before the final gate by leaving `canManage` false and adding no action gate. Typecheck and lint pass; the complete unit suite passes with 343 files, 3,032 tests passed and three intentional skips; the production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. Existing hydration and cancelled-request warnings did not fail the suite. These checks prove source and test environments, not the independently running app.
- Application source `65b419e6949f54bbed06dff885a32f8e8ea05527` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged Notifications heading, header bell and empty state. A separately signed-in fictional Member opens the same exact route and sees the unchanged heading and header bell. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Trust Center — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module is now the single source for the exact private Trust Center route, Owner/Admin management permission, operator Share navigation metadata and action denial. Owners and Admins retain the same settings query, form and save/switch-off actions; Members remain redirected before reaching the private page. The anonymous `/trust/[slug]` route and its safe public database function remain outside workspace membership policy. Every visible word, thrown error, redirect, validation rule, rate limit, database call, query shape and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: seven access-contract and source-ownership cases failed first while Trust Center was absent and its decisions were scattered; the focused Trust Center/access/navigation suite then passed with 5 files and 112 tests. Independent read-only review found no behavior drift, access widening, missed private caller or duplicated navigation metadata. Typecheck and lint pass; the complete unit suite passes with 342 files, 3,023 tests passed and three intentional skips; the production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. The run emitted existing hydration and cancelled-request warnings but no test failed. These checks prove the source and test environments, not the independently running app.
- Application source `e2de640599140f1bc21e9645eaaeca9b1bb771ad` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged private Trust Center heading and setup controls. A separately signed-in fictional Member is redirected from `/app/trust` to `/app` and sees no private Trust Center content. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Scope and context — 14 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module is now the single source for the exact Scope route, Owner/Admin page visibility, owner-only editing presentation and owner-only save authorization. Owners retain the same editable six-field form; Admins retain the same disabled fields and exact read-only explanation; Members remain redirected before reaching the page. The section still has no sidebar item, and the existing shell-header fallback remains unchanged. Every visible word, thrown error, redirect, validation rule, rate limit, database call, query shape and cache invalidation is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: four access-contract cases and one source-ownership case failed first while Scope was absent and its decisions were still scattered; the focused Scope/access/portal/baseline-link suite then passed with 7 files and 150 tests. Typecheck and lint pass; the complete unit suite passes with 341 files, 3,013 tests passed and three intentional skips; the production build passes. The required workspace-UI and team-contribution browser suite passes 8/8 across Chromium desktop and mobile against the isolated fictional test environment. The run emitted existing hydration warnings but no test failed. These checks prove the source and test environments, not the independently running app.
- Application source `315c15e8753002b98c07c671f1eb24620accba89` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged Scope heading, six editable fields and `Save scope profile` control. A separately signed-in fictional Member is redirected from `/app/scope` to `/app` and sees no Scope heading or controls. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. Database RLS still permits Members to read the underlying row, but the reachable application route intentionally remains unavailable to Members; this refactor did not widen it. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Saved baseline — 13 September 2026

- Local-only architecture refactor: the organisations-domain Workspace-access module is now the single source for the exact Saved-baseline route, Owner/Admin and Member page presentation, and save authorization. Owners and Admins retain the same form, progress and dated-save controls; Members retain the same saved read-only explanation and history. The section still has no sidebar item, and the existing shell-header fallback remains unchanged. Every visible word, return shape, redirect, validation, rate limit, database call, query shape, cache invalidation and error message is unchanged. No product behavior, feature or existing test was deleted.
- Fresh source verification on `codex/team-baseline`: four access-contract cases and two source-ownership cases failed first while the section was absent and its decisions were still scattered; the focused Saved-baseline/access/navigation suite then passed with 11 files and 147 tests. Typecheck and lint pass; the complete unit suite passes with 340 files, 3,006 tests passed and three intentional skips; the production build passes. After the isolated local environment was correctly sourced, the required workspace-UI and team-contribution browser suite passed 8/8 across Chromium desktop and mobile. The run emitted existing hydration warnings but no test failed. These checks prove the source and test environments, not the independently running app.
- Application source `a3a638201a7261c40e438e9785629aa0e85accac` is pushed and its rebuilt immutable production package runs independently at `http://127.0.0.1:3300` with a mode-0600 log against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA, and the unauthenticated sign-in route returns its expected page. An authenticated Owner sees the unchanged Saved-baseline form and save controls. A separately signed-in fictional Member sees the Saved baseline without those controls and is redirected from `/app/baseline/edit` to `/app`. The validated disposable Member's local authentication password was reset for this check; the live check changed no business record. The browser suite created only its designed disposable fictional test records. The first app replacement met a port-release race, so the known-good app was restored before an isolated new-package probe; waiting for port 3300 to close then produced the healthy exact-source process. This is the eighth gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Controls & applicability — 13 September 2026

- Local-only architecture refactor: the organisations-domain Workspace access module is now the single source for Statement-of-Applicability list/detail/import and export route decisions, Owner/Admin and Member navigation metadata, page presentation, finalisation authorization and SoA-import authorization. Members retain the same read-only list/detail access and cannot import, export, edit, create successors or finalise; Owners and Admins retain the same management controls. The existing create/review/successor action behavior is unchanged, with no new early role denial added where none existed. Every visible word, redirect, blocked response, validation, rate limit, database call and error message is unchanged, and no product record, feature or existing test was deleted.
- Fresh local verification on `codex/team-baseline`: four new SoA access-contract cases failed while the section was absent, then the focused access/SoA/import/navigation suite passed with 12 files and 197 tests. Typecheck and lint pass; the complete unit suite passes with 339 files, 3,000 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. The browser run emitted existing hydration and cancelled-request warnings but no test failed. These automated checks verify source and test environments, not the independently running app.
- Application source `03a69cb4ef1605bbab395c7e90f75ff5c2a28b38` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA; the sign-in route loads, an authenticated Owner sees the unchanged SoA page and operator controls, and a separate fresh fictional Member form sign-in sees `Member view`, the exact existing read-only explanation and no import/export/successor controls. That Member is redirected from `/app/soa/import` to `/app`, and the authenticated export API returns HTTP 403. The first package was rejected when health exposed an old build-time local database URL; the known-good app was restored, then the same source was rebuilt with the resumed local environment and passed. Only the application process and disposable users' local authentication passwords changed; business records and database volumes were not changed. This is the seventh gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Policy library — 13 September 2026

- Local-only architecture refactor: the organisations-domain Workspace access module is now the single source for Policy-library list/detail/new route decisions, Owner/Admin and Member navigation metadata, page presentation, policy authoring and approval actions, policy-evidence management and feedback-resolution authorization. Members retain the same read-and-accept experience and contribution actions; Owners and Admins retain the same authoring, approval, status, evidence and feedback controls. Every visible word, redirect, blocked response, validation, rate limit, database call and error message is unchanged, and no product record, feature or existing test was deleted.
- Fresh local verification on `codex/team-baseline`: the new policy access contract failed first while the Policy section was absent, then the focused Policy/access/navigation suite passed with 20 files and 176 tests. Typecheck and lint pass; the complete unit suite passes with 339 files, 2,996 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. The browser run emitted existing hydration warnings but no test failed. These automated checks verify source and test environments, not the independently running app.
- Application source `a95ad033c17885093cf6f3b7a9d8cdac234419ad` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA; the sign-in route loads, an authenticated Owner sees the unchanged Policy library and its `New policy` control, and a separate fresh fictional Member browser session sees Policy navigation, `Member view` and personal acceptance presentation without the authoring control, then is redirected from `/app/policies/new` to `/app`. The retained disposable fictional Member fixture was reused by changing only its local authentication password; saved showcase and business records were not changed. This is the sixth gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Asset inventory — 13 September 2026

- Local-only architecture refactor: the organisations-domain Workspace access module is now the single source for Asset-inventory list/detail/new/edit/import and export route decisions, Owner/Admin navigation metadata, page controls, Asset mutations and Asset-import authorization. Owners and Admins retain the same access and controls; Members still have no Asset-inventory navigation or route/API/action access. Every visible word, redirect, blocked response, validation, rate limit, database call and error message is unchanged, and no product record, feature or existing test was deleted.
- Fresh local verification on `codex/team-baseline`: the new access contract and source-seam tests failed while the Asset section was absent, then the focused Asset/access/import/export/navigation suite passed with 13 files and 163 tests. Typecheck and lint pass; the complete unit suite passes with 339 files, 2,992 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. The browser run emitted existing hydration warnings but no test failed. These automated checks verify source and test environments, not the independently running app.
- Application source `9e14906f05eb6b8bfffb046e2b907dccf5862c72` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA; the sign-in route loads, an authenticated Owner sees the unchanged Asset inventory and its operator controls, and a separate fresh fictional Member browser session is redirected from `/app/assets` to `/app`, shows `Member view`, and has no Asset-inventory navigation or page heading. The retained disposable fictional Member fixture from the previous access check was reused by changing only its local authentication password; saved showcase and business records were not changed. This is the fifth gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Risk register — 13 September 2026

- Local-only architecture refactor: the organisations-domain Workspace access module is now the single source for Risk-register list/detail/new/edit/import and export route decisions, Owner/Admin navigation metadata, page controls, risk CRUD, assessment-gap acceptance, risk-matrix configuration, treatment-plan actions and risk-import authorization. Owners and Admins retain the same access and controls; Members still have no Risk-register navigation or route/API/action access. Every visible word, redirect, blocked response, validation, rate limit, database call and error message is unchanged, and no product record, feature or existing test was deleted.
- Fresh local verification on `codex/team-baseline`: the new access contract and source-seam tests failed while the Risk section was absent, then the focused Risk/access/import/navigation suite passed with 17 files and 182 tests. Typecheck and lint pass; the complete unit suite passes with 338 files, 2,974 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. The browser run emitted existing Tasks/Evidence hydration warnings but no test failed. These automated checks verify source and test environments, not the independently running app.
- Application source `0b818ad8f3bbe1ae9443d96bdabb5cef2caade96` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the isolated fictional team database. Fresh health reports application and database `ok` with that exact SHA; the sign-in route loads, an authenticated Owner sees the unchanged Risk register and its operator controls, and a separate fresh fictional Member browser session is redirected from `/app/risks` to `/app`, shows `Member view`, and has no Risk-register navigation or page heading. One disposable fictional Member-check workspace remains in the isolated test database because its immutable-audit and last-owner safeguards correctly rejected cleanup; saved showcase records were not changed. This is the fourth gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Readiness assessments — 12 September 2026

- Local-only architecture refactor: the organisations-domain Workspace access module is now the single source for Readiness-assessment list/detail route visibility, operator navigation metadata, Owner/Admin management capability, page presentation and the completion endpoint's authorization and exact denial message. Members still open the same list and detail pages in read-only form and still have no Assessment navigation item; Owners and Admins retain the same navigation, creation, editing, control-review and completion controls. Every visible word, route, response, validation, rate limit and database call is unchanged; no record, feature or test was deleted.
- Fresh local verification on `codex/team-baseline`: the new access contract failed first while the section was absent, then passed after implementation; the focused Assessment/access seams pass with 9 files and 113 tests. Typecheck and lint pass; the complete unit suite passes with 337 files, 2,954 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. An extra whole connected-controls run reached an unrelated existing SoA colour-contrast assertion before its serial Member case; the required setup plus Member read-only Assessment pair then passed 2/2 unchanged. Automated checks remain separate from the independently running app.
- Application source `e269f59e93a9f62f60520835cf3addc275d8050e` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the isolated fictional team database. Fresh health reports application and database `ok` with that SHA; the sign-in page and operator Assessment register load, and a separate fresh fictional Member browser session renders the Assessment page with the existing empty-state wording, no creation control and no Assessment navigation link. This is the third gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Leadership report — 12 September 2026

- Local-only architecture refactor: the organisations-domain Workspace access module is now the single source for Leadership report route and PDF visibility, Owner/Admin versus Member presentation, navigation metadata and publishing authorization. Members still see only the latest published snapshot and its PDF, while Owners and Admins retain the live report and publishing access. Every route, label, response, rate limit and exact denial message is unchanged; no record, feature or existing test was deleted.
- Fresh local verification on `codex/team-baseline`: the new access contract failed first because the section was absent, then passed after the policy was added; all six focused seams pass with 68 tests. Typecheck and lint pass; the complete unit suite passes with 337 files, 2,950 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. These automated checks verify source and test environments, not the independently running app.
- Application source `de9889198aa119086dd65611bcbcb8d6f5a586a0` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the fictional local team database. Fresh health reports application and database `ok` with that SHA; the sign-in route loads, and a fresh fictional Member browser session sees the published Leadership report without a publish control and downloads the authenticated PDF successfully. This is the second gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Shared Workspace access — Framework coverage — 12 September 2026

- Local-only architecture refactor: the new organisations-domain Workspace access module is now the single source for Framework coverage route visibility, Member navigation metadata, page editability and action authorization. Members still see and read Framework coverage but cannot add or remove mappings; Owners and Admins retain management access. The route, labels and exact denial message are unchanged, and no record, feature or test was deleted.
- Fresh local verification on `codex/team-baseline`: typecheck and lint pass; the complete unit suite passes with 337 files, 2,946 tests passed and three intentional skips; the production build passes. The existing workspace UI and team-contribution Member journeys pass 8/8 across Chromium desktop and mobile. These automated results verify the source and test environments, not the independently running app.
- Application source `fb29793075738b68e888d99144a722c737f76b78` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the fictional local team database. Fresh health reports application and database `ok` with that SHA, the sign-in route loads, and a fresh fictional Member browser session sees Framework coverage navigation and a recorded mapping without Add/Remove controls; the same session is redirected away from operator-only Settings. This is phase one of a gradual access-rule migration with no visible behavior change. No database schema, staging, hosted release, live-provider or acceptance gate changed.

### Feature-owned server action relocation — 12 September 2026

- Local-only architecture refactor: 18 existing server-action implementations moved byte-for-byte from the mixed `src/app/app/actions.ts` file into the risks, SoA and organisations feature modules. The old path remains a thin async forwarding interface, so existing pages and components keep the same imports. No feature, record, validation, wording, rate limit, query or error behavior changed, and the six action test files remained byte-identical.
- Fresh local verification on `codex/team-baseline`: typecheck and lint pass; all six action seam suites pass with 57 tests; the complete unit suite passes with 336 files, 2,942 tests passed and three intentional skips; the production build passes. The first complete run exposed three legacy source-scope checks tied to the old file location; their documented tenant-scope signatures remain at the public boundary and the unchanged contract passes in the complete rerun.
- Application source `ff24aa597e6f03368a3ad3ec02e1aa074d83e363` is pushed and its rebuilt production package runs independently at `http://127.0.0.1:3300` with private file logging against the preserved fictional team database. Fresh health reports application and database `ok` with that SHA; the sign-in route loads and an authenticated SoA review renders the existing 93-control catalogue, review state and blocker summary. This is a relocation with no visible change. No database, staging, hosted release, live-provider or acceptance gate changed.

### SoA finalisation rule ownership — 12 September 2026

- Local-only architecture refactor: `src/features/soa/application/finalisation.ts` now owns the catalogue size, evidence-freshness classification and finalisation preflight data gathering used by the action and review loader. The two existing blocker messages retain their exact wording, no records or features were removed, and the rendered workflow is unchanged.
- Fresh local verification on `codex/team-baseline`: typecheck and lint pass; the full unit suite passes with 336 files, 2,942 tests passed and three intentional skips; the production build passes. The first unrestricted test runs exposed two unrelated timing failures under Mac resource contention. One real process-readiness race in the resource-guard test was stabilised; the Evidence test passed alone and the complete suite then passed with the documented single-worker setting.
- The rebuilt production app runs independently at `http://127.0.0.1:3300` with private file logging against the preserved fictional team database. Fresh health reports application and database `ok`; the sign-in page loads and the existing authenticated SoA review renders its 93-control catalogue and blocker summary. This is local fictional verification only. No database, staging, hosted release, live-provider or acceptance gate changed.

## Active goal — connected remediation rehearsal

- The attached overnight goal resumes the full lifecycle work; the earlier usability-only pause below is historical, not a current instruction. Preserve the polished layout and existing fixtures.
- Current-state check: branch `codex/compliancehub-internal-tool`, source and running health SHA `bba92bd6ab1400ab63f68865435f328652343dc0`; health reports database OK. Build metadata identifies local project `compliancehub`, API `http://127.0.0.1:54321`, migration `20260905013000`. Tracked tree was clean before this checklist update; historical untracked artifacts remain untouched.
- Fresh baseline: monitoring runner/provider/actions **3 files / 24 tests pass**; audit action scope **1 file / 7 tests pass**. These are focused unit checks, not browser lifecycle acceptance. An initial command included a nonexistent audit test filename; the actual `actions.scope.test.ts` was then run explicitly.
- Source confirms sandbox posture is fixed, official GitHub resolution requires a newer fresh pass, and audit finding closure is a separate human action. An independent read-only Astra review is tracing the supported end-to-end path before selecting the smallest truthful rehearsal. No task completion or finding resolution has been simulated as live verification.
- Actual Browser inspection confirmed Northstar Automation offers baseline generation with zero configured systems; Monitoring correctly reports no connection and unconfirmed status. Source inspection also found swallowed manual collection failures and ignored Automation page query errors. Focused corrections are underway without changing the layout or enabling providers.
- Baseline action regression reproduced four missing outcomes before the change. It now redirects to the existing message area with empty, completed, partial or failed collection feedback, retains successful-source drafts and hides raw errors. **2 files / 12 tests pass** including original action regressions. Production rebuild/browser verification is still pending; the running app remains the earlier release.
- Existing GitHub parser/control-room/provenance/action baseline: **4 files / 106 tests pass**. This does not establish live provider connectivity or replace database materialisation evidence.
- Independent Astra review selected a new separately named fictional human-review audit using existing forms. Audit finding closure remains an explicit operator decision, not an enforced fresh-evidence gate. A source-only rerun-safe setup extension is in progress; no showcase record has yet changed.
- Automation source group committed as `215d1a2`: availability/query-error handling, clear fictional collector label, shared deterministic collection timestamps and manual baseline outcome feedback. Combined page/action checks **3 files / 21 tests pass**, scoped ESLint and full typecheck pass. One interim date-label assertion failed after adopting the shared formatter and was corrected; this is not production acceptance yet.
- Fresh observability storage-failure/route/limiter regression: **3 files / 26 tests pass**. Existing database privilege evidence remains applicable to unchanged migrations; hosted gates remain unverified and no hosted operation was attempted.
- First actual human-review run created only the new NS-AUD-002 audit, then exposed a setup readback race. A private read-only snapshot minus that exact inserted audit matched all **17** original table fingerprints/counts; the original pending intent was restored without database writes. Evidence: `artifacts/human-review-2026-09-06/timing-recovery.json` and private snapshot/reconciliation script. The incorrect initial “did not persist” result is retained in `setup-run.log`.
- Recovery correctly accepted NS-AUD-002 and created one checklist. Waiting for the whole Next action response then stalled despite a committed checklist and healthy app. Only confirmed setup PID39996 was terminated; its terminal tool result and dead PID were verified before removing its lock. Pending checklist intent remains preserved. Bounded exact readback polling is being substituted for unbounded response completion; no action is being blindly replayed.
- Supported UI rehearsal completed all 11 human-review steps: separate NS-AUD-002, non-compliant checklist, atomic owned finding/task, Done task while finding remained in progress, new fictional sample review evidence with dated validity, three evidence links, compliant human review, explicit finding closure. Current audit/report exports succeeded. `setup-task-selector.log` records successful execution; two pre-submit selector failures and exact unchanged-data recoveries are retained separately. Runtime is still `bba92bd`; new Automation code is not yet deployed locally.
- Actual in-app Browser shows both original NS-AUD-001 and new NS-AUD-002; one original finding remains open and zero open non-conformities. Follow-up audit status is still Planned and needs progression to Reporting before final presentation. Report Member snapshot publication and exact new-build desktop/mobile acceptance remain outstanding.
- Normal human-review setup rerun passed (`setup-rerun.log`) without repeating transitions. Actual Browser opened NS-AUD-002 and confirmed the compliant checklist, closed finding, linked current evidence and explicit fictional-review limitation. The operator guide now documents this journey, two-separator setup commands and pending-action recovery rules.
- Current read-only local permission matrix still matches Jira service-only credentials, service-only counter execution, and restricted audit-log grants (`current-permission-matrix.json`). Fresh npm audit retains **0 high/critical, 2 moderate entries** (ExcelJS/UUID); the earlier UUID v4 scope analysis remains the documented limitation. No dependency override or hosted mutation was made.
- Reporting extension now completed through supported UI: NS-AUD-002 status Reporting and a separate new Member leadership snapshot. Original snapshot content is preserved, baseline setup pins its saved identity, and verify-only passes with exactly two snapshots (`setup-reporting.log`, `verify-reporting.log`). Combined new setup/recovery and Automation regressions **5 files / 36 tests pass**, full typecheck passes. Production-browser acceptance on the new candidate is next.
- Candidate **16c25f281a4ddea1f313d43872dd6e183442bad7** built successfully and passed **8/8 production browser tests in33.4s, zero retries**, covering desktop/mobile original + human-review journeys, all-section navigation, Member restrictions and exports. Same-build production restart, fresh password sign-in and authenticated Reporting-audit refresh passed. Evidence: `artifacts/human-review-2026-09-06/acceptance-candidate.json`, browser/build logs, `fresh-login.json`. Full lint/typecheck and36focused tests pass.
- Independent narrow Astra review found no blocking journal/report-preservation issue. Clarification: finding→checklist association is through the same audit and explicit immutable evidence references; the finding form has no direct checklist selector. Same fictional operator records review; neither reviewer independence nor an enforced closure prerequisite is claimed. Final documentation/package identity and staging handoff remain to be consolidated.
- [x] Demonstrate the explicitly fictional human-review lifecycle and independent statuses through supported local workflows, with rerun-safe setup. Live GitHub remains outside this local demonstration.
- [x] Verify Monitoring/Automation availability, role and error behavior through actual navigation and focused failure regressions; retain database security proof and recheck current grants. Failure-path unit tests are not presented as a live provider exercise.
- Final preservation check: all9historical reference count/content fingerprints match using the original SQL **newline** delimiter (`reference-preservation-newline.json`). The initial literal-backslash delimiter comparison is retained and rejected as an algorithm mismatch, not data drift. No historical records were repaired or overwritten.
- Staging read-only recheck: current CLI default account belongs to another subscription/tenant; an explicit request to the saved target subscription still fails **AADSTS530035** (`azure-explicit-target.log`). No account switch, hosted configuration write, migration or deployment occurred. This is the first renewed blocked observation after completion of independent local work; final packaging continues.
- [x] Package local production acceptance, source archive and conditional staging handoff. Exact identity and checksum are in `artifacts/human-review-2026-09-06/release-package.json`; hosted execution is not approved or verified.
- [x] Packaged-build throttled loading→loaded monitoring, missing-risk recovery and missing export404 pass (`loading-errors.json`). Actual Browser confirms no baseline action with zero sources. The operator guide includes the live GitHub rehearsal requiring an approved disposable target and local source rollback instructions.
- [ ] Obtain interactive access to the saved Azure tenant and capture live image/configuration/recovery details before requesting deployment approval. Live GitHub target/access remains separately unapproved; no external notifications or hosted writes occurred.

## Earlier priority — whole-app clarity polish, 2026-09-06

- [x] Inspect all 15 existing main navigation sections with fictional Northstar data and research official Vanta/Drata workflows. Findings and source links extend the existing `docs/design-review/2026-07-06-full-app-design-and-feature-draft.md`; no competing plan created.
- [x] Implement precise dashboard/report metrics, clear assessment/control/risk guidance, readable Evidence and truthful empty states. Preserve application workflows and historical data.
- [x] Production source `bb10dba` passed 20 focused files / 95 tests, lint, typecheck, build and all 6 production browser tests (31.8s, zero retries). Browser tests cover all 15 main navigation sections on desktop/mobile, the existing connected journey with exports, Member restrictions, Evidence maintenance disclosures and the populated fictional performance trend. Two observed mobile failures (SoA form and Settings team controls) were corrected and rerun. Updated old-label test failures and the initial interrupted attempt remain preserved.
- [x] Supported setup added one fictional manual performance measure and two dated readings (85% → 95%, target 95%). Normal rerun and verify-only passed without duplicates; existing Northstar task/finding statuses remain unchanged. Historical reference count/content hashes match across all 9 checked tables; private credentials remain mode 0600.
- Evidence and exact final runtime identity: `artifacts/clarity-polish-2026-09-06/acceptance.json`, build/test/setup logs and `reference-preserved.json`. The later checklist-only commit requires a matching rebuild/restart; acceptance records both functional-test SHA and final running SHA.
- Limitations: this is the existing local app with fictional data. Section loading does not prove every possible mutation. GitHub/provider configuration stays disabled; no public sharing, notification, hosted migration or deployment was performed. The original server-stall trigger, full fresh-verification lifecycle and staging gates are not closed by these changes.

## Earlier priority — existing workflow usability, 2026-09-05

### Latest user correction — restore the familiar Evidence experience

- 2026-09-06 continuation: local production health still reports `8f19bbf9f213292239acb5d444a6e377e66e366d` with database OK. Actual in-app Browser followed Evidence → owned In Progress task → selected note (automatically expanded) → audit checklist → leadership report. The non-compliant checklist, open observation, one open task and two current evidence records remain distinct; no status or business record was changed. Mobile Evidence measures 390px content in a 390px viewport and retains collapsed mappings/editing controls.
- Astra investigated the old native sample: 522/561 main-thread samples were in exception handling. An isolated subprocess using the installed Next environment and exception logger did not reproduce the loop with either drained or closed stderr; both handled one exception and exited normally. Repeated disabled-MCP OAuth errors coexist with a healthy server and do not establish causation. File-backed logs are an operational safeguard, not a proven fix.
- Updated the existing operator guide with bounded health checks, private log-file startup, application-only recovery and explicit separation of current build identity from historical acceptance. The complete fictional verification lifecycle and hosted gates remain deferred; this continuation does not close them.

- Astra retry completed successfully. Follow-up review found a pre-existing boundary error: 200 recent evidence records plus an older selected item exceeded the provenance loader's 200-ID limit. The selected item now replaces the last displayed row; no record is deleted. A full-size regression reproduces the failure before the fix.
- A later local health check found the old Next process unresponsive at 100% CPU while Supabase remained healthy. The native sample records repeated exception/log handling; the initiating error is not yet established. Only the application process was stopped, and new runtime output goes to a local log file. Recovery checks and exact tested build are recorded in `artifacts/runtime-check-2026-09-05/`; this is not a claimed root-cause fix for the stall.

The user has paused the fictional resolution scenario and all feature expansion. The immediate priority is restoring the basic Evidence experience from the existing demo/design history.

- [x] Compared the current populated page, git history (`b48beba`, `161d9d1`, `5e15b0b`, `1a90596`), July UI uplift/design notes, backlog and archived demo screenshots. The main clutter is the 93-control baseline displayed as full chips with removal buttons, plus the large freshness donut. Rolling back only the latest linkage commit would leave that problem intact.
- [x] Restore compact freshness summaries and readable responsive headings. Keep small useful record links visible; open long control mappings and link-editing forms only when requested. Preserve exact evidence IDs, notes, immutable records, working navigation and server actions. Official GitHub evidence uses reviewed readable check labels and collapses technical metadata; raw provider titles remain excluded.
- [x] Correct cached workspace/account labels after another tab signs into a different account. The retained layout refreshes from the server on account changes, focus, visibility and browser-history restoration. Browser data never grants access or supplies workspace labels. No database records, roles or memberships were changed.
- [x] Focused tests: 7 files / 46 tests pass, including 93-control disclosure, readable official provenance, existing tenant/Member restrictions and session-refresh behavior. Earlier failure logs retained. Exact production build/browser acceptance is recorded privately under `artifacts/evidence-restoration-2026-09-05/`; those runtime checks are required before calling this correction complete.
- [ ] Full remediation-to-fresh-verification scenario remains deferred. Do not revive the earlier product-choice question or add integrations while the user is asking for basic usability restoration.


The user has deferred live GitHub enablement. Continue the existing app and fictional showcase; the earlier RC3 acceptance below does **not** establish the complete finding → task → remediation → fresh verification → evidence → audit/report workflow.

- [x] Confirm reference runtime: health reports `11a8fd5e5a4666051c346b097350468b915a4347`, local API `127.0.0.1:54321`, project `compliancehub`, migration `20260905013000`. Existing tags, historical fixture and private artifacts preserved.
- [x] Browser inspected Safari **ComplianceHub Local Demo** (Owner): five active saved official GitHub findings, none of these five linked to a remediation task; 15 recorded checks (8 pass, 5 fail, 2 unknown). The current runtime lacks GitHub configuration; a prior fresh-check attempt returned a generic retry error. Stored observations are not proof of a currently operational connection. No reference record/integration changed.
- [x] Browser inspected **Northstar Demo — Showcase v1** (Owner): no monitor sources/findings; its existing risk treatment task retains owner, 2026-12-31 due date, risk/control/evidence links. Task completion and finding verification use independent application logic.
- [x] Source traced concrete gaps: task detail omits originating monitoring/audit findings; immutable note evidence description is not rendered; done tasks disappear from evidence-link choices; supported audit checklist evidence-link action has no UI. Sandbox monitoring always returns a fixed failing posture, so fixture expansion alone cannot demonstrate a fresh passing recheck.
- [x] Verify usability fixes in a new exact production build: configuration-aware GitHub feedback; task/finding navigation and independent statuses; readable evidence notes and post-completion evidence links; audit checklist evidence links.
- [ ] Complete the fictional lifecycle and rerun-safe setup. Focused product choice requested: use existing human audit verification first, or extend the existing fictional sandbox with an explicit recheck control. Neither path may be presented as a live provider check.
- [ ] Desktop/mobile lifecycle, fresh sign-in, refresh/restart, loading/error/role checks; capture exact new source/runtime and evidence, commit intended groups and update the existing operator guide.
- [ ] Hosted staging remains a separate Azure authentication/approval gate. No hosted migration or deployment is authorised.

### Usability checkpoint evidence

- Source groups: `0a99b30` (unavailable GitHub checks and honest partial-result feedback), `1a90596` (task/finding navigation, readable notes, post-completion evidence linking, audit checklist links and export contents), `f723727` (supported showcase evidence-to-audit setup and operator explanation), `7458f75561ba24933dc2aada8b15c316e5233ae1` (stable monitoring dates across Node/Safari). Previous release tags remain unchanged; this checkpoint is **not** acceptance of the full fictional resolution lifecycle.
- Production source `7458f75` rehearsed at `http://127.0.0.1:3100`, API `http://127.0.0.1:54321`, local project/container `compliancehub` / `supabase_db_compliancehub`, Colima, PostgreSQL 15.8, 129 migrations through `20260905013000`. Provider credentials and outbound notifications remain disabled. Final current-build identity is captured in `.next/local-demo-build.json` and the private `artifacts/workflow-usability-2026-09-05/checkpoint.json`; any later documentation commit must be rebuilt before startup.
- Meaningful regression results: initial usability changes **16 files / 84 tests**; subsequent date correction **3 files / 33 tests**, including overlapping component tests (these totals are not additive). Full lint and typecheck passed before the date change; its focused lint/typecheck passed. Production build and **4/4 desktop/mobile journey and Member permission tests** passed on `7458f75` (16.0s). Fresh password sign-in, assessment 10/10, audit activity, leadership report and authenticated refresh passed. No schema changes or full database-suite replay was needed.
- Safari exposed React #418 after the new build: Node rendered `Sept` and a comma, Safari `Sep` and `at`. Shared numeric London date parts with fixed English labels remove this server/browser mismatch. A component regression failed before the fix; populated Safari then loaded correctly on two refreshes with an empty console. Temporary cache override was removed and inspector closed. This fixes the observed mismatch; it is not a blanket cross-browser performance claim.
- Actual in-app Browser followed task → selected evidence → readable Evidence details → linked audit checklist. The audit pack now includes linked evidence metadata and note contents, and returns an error if that data cannot be loaded. An unavailable selected evidence record has explicit feedback. Task Done, finding state and evidence freshness remain independent.
- Showcase setup initially stopped on a pre-existing task edit. Audit history and an exact old fingerprint reconstruction proved only `open` → `in_progress` plus its update time had changed before this work. Preserved that task state, backed up the private manifest, and reconciled only its checkpoint fingerprint. Supported setup added one evidence-to-checklist link; normal rerun and verify-only passed, leaving 2 evidence items and 98 links. Future drift must still be reviewed. Evidence: `accepted-existing-task-progress.json`, setup logs, private before-manifest.
- Historical reference preservation: exact count/content hashes of all nine checked tables match before/after (`reference-before.json`, `reference-after.json`). No reference task, finding, evidence, audit or integration was mutated.
- Still open: Northstar has no monitoring source or finding; the existing sandbox returns fixed failures. A fully demonstrated remediation → new verification → resolution scenario awaits the requested human-review versus fictional-recheck choice. No saved observation is relabelled live, and no task was marked Done just to improve the demo. Hosted staging remains blocked separately.

- [ ] Project owner has reviewed and approved every assessment question and remediation.
- [ ] Legal, privacy, certification, and open-source disclaimers are visible and accurate.
- [ ] Production Supabase project uses the intended region and all migrations pass.
- [ ] Hosted Supabase personal staging project ref `ytenjiyjdcrjkgwmciqw` has a verified backup; `supabase migration list` and `supabase db push --dry-run` showed exactly the twenty-four reviewed migrations through `20260825124025` in order; migrations 1–19 were verified before the manual bridge, additive migrations 20–24 were verified before final, and the phase-specific `HOSTED_SUPABASE_MIGRATION_VERSION` always matched `NEXT_PUBLIC_SUPABASE_URL`.
- [ ] Cross-tenant RLS tests, immutable-record tests, and stale-write tests pass.
- [ ] Service-role and cron credentials are server-only, rotated, and stored in deployment secrets.
- [ ] Mukta has verified one Mukta-owned, server-approved private Slack destination; its canonical webhook SHA-256 is stored only in `SLACK_ALLOWED_WEBHOOK_SHA256`, the A/B secret references are coherent, and blank/malformed/mismatched/legacy configurations fail without a Slack write.
- [ ] The staged Slack rollout completed in order: manual `bridge` on migration `20260825040825`, health capability `v1`/`bridge` with the exact release SHA, additive migrations `20260825053718`, `20260825073650`, `20260825082411`, `20260825094343`, and `20260825124025`, then manual `final` in `strict` mode. The first final followed the exact bridge revision; in steady-state operation, subsequent manual final and automatic runs stayed strict while accepting only an exact `v1` predecessor whose captured mode was `bridge` or `strict`. Final had exact same-slot Slack/core references, and rollback was pinned to the captured revision/image/references and matched its exact previous mode and release SHA.
- [ ] An Adtecher organisation owner approved the private GitHub App, one dedicated selected repository, exact read-only permissions/events, SSL verification, OAuth-during-install disabled, Redirect-on-update enabled, and the callback/setup/webhook URLs at the canonical origin.
- [ ] `REGISTERED_GITHUB_APP_SITE_URL` exactly equals `NEXT_PUBLIC_SITE_URL` and that origin exactly matches the Azure Container App ingress FQDN; all eight server-only GitHub values are present under their valid `AZURE_GITHUB_*` secret aliases in the protected personal `azure-staging` environment; the private key is PKCS#8 with literal escaped `\n`; no GitHub value is present in build inputs, local files, or client output.
- [ ] Do not merge to `main` or deploy while either hosted Supabase or GitHub App checkpoint above is incomplete; `main` auto-deploys after CI and the environment currently has no required reviewer gate.
- [ ] Email delivery, allowed redirect URLs, rate limits, and abuse monitoring are configured.
- [ ] Backup restoration has been exercised into a separate environment.
- [ ] Desktop and mobile critical journeys pass keyboard and automated accessibility checks.
- [ ] PDF and DOCX exports match the finalised snapshot.
- [ ] `npm run verify`, `npm run test:db`, and `npm run test:e2e` pass from a clean checkout.

## Local verification evidence — 2026-08-18

These checks prove the current local implementation only; they do not close the
hosted, Azure, Slack, GitHub-owner, email, or backup checkpoints above.

- [x] On the current hardening commit `5ba9586`, the release verification
  baseline passed lint, typecheck, **210 test files / 1,472 tests**, and the
  Next production build; the focused export, policy-evidence, monitoring
  owner/RLS, risk-matrix operator, audit-scoping, filename, active-workspace
  GitHub repository-selection, atomic KPI task, and atomic monitoring-task
  tests are included in that count. Assessment autosave, automation review,
  and SoA finalisation now return stable client errors instead of raw database
  details.
- [x] GitHub CI run `32158868448` for `3fd6ebd` passed Gitleaks, container,
  database upgrade/full pgTAP, application lint/typecheck/unit/build,
  integration, and the full desktop/mobile Playwright gate.
- [x] The CI database job ran `bash scripts/test-db-upgrade.sh` and
  `supabase test db` successfully. The local fixture-preserving DB run also
  passed `supabase test db`: **79 files / 1,257 tests**, including the atomic
  KPI and monitoring finding task RPCs plus operator-only risk mutations.
- [x] Fresh local `npm run test:integration`: 3 files / 5 tests against the
  running localhost Supabase stack.
- [x] GitHub CI run `32154415336`: **58/58 desktop + mobile tests passed**
  against the disposable CI fixture with one worker.
- [x] Local `/api/health` returned HTTP 200 with `db: ok`; the running local
  stack reports 101 applied migrations and the expected GitHub/automation/MCP
  tables are present.
- [x] Focused GitHub production shadow run: Chromium + mobile 2/2; local
  personal-pilot readiness unchanged.
- [ ] No earlier third-party workspace/channel smoke is accepted as release
  evidence. Prove the application-owned digest and monitoring paths only against
  Mukta's server-approved private Slack destination after the owner checkpoint.
- [x] `npm run test:db:upgrade`: passed earlier in the disposable/local upgrade
  gate (14 upgrade assertions, with the database restored afterwards); it was
  not rerun in this final fixture-preserving pass.

## Demo release continuation — 2026-09-05

This is the active release checklist. Earlier checked audit items are historical
evidence, not acceptance of this release. Continue from the audit and branch
reconciliation documents; preserve unfinished branches and the frozen CEO fixture.

1. [x] Package intended audited source, tests, migrations and documentation into reviewed commits; initial candidate `8785a4fa84e30b5596763068b00a03848a15d282` tagged `demo-rc-20260905-1`. Later demo/security fixes require an updated final release SHA.
2. [x] Add a fictional showcase through supported workflows; prove reruns preserve IDs/counts and historical data.
3. [x] Rehearse an identified production build/database in Browser on desktop/mobile, including restart, refresh, errors, loading and permissions.
4. [x] Verify auditor-link database limits, Jira credential grants, observability storage failure and dependency advisories.
5. [ ] Prepare exact staging target, migration/backup/health/rollback evidence; request approval before hosted changes; deploy and rehearse only after approval.

### Release evidence and decisions

- Starting source: `dcded8801bd57657702bd91f91b5306004b673c4`, branch `codex/compliancehub-internal-tool`; current audited changes were uncommitted. Work continues in this existing checkout to preserve unfinished changes.
- Initial tracked patch, untracked archive and SHA-256 inventory saved locally under `artifacts/release-2026-09-05/`; historical screenshots remain in place.
- Historical fixture source SHA-256: `9eaa52b99a43f3a99afdc8893a4e6292e7e4bf91e0ed7a38f3220e8716992bef`. Its incomplete completed assessment is a preserved historical limitation.
- Existing `.env.local` targets hosted Supabase `etpiqjbehbchkkzmbjzt` and a stale `http://localhost:3000` origin. Do not use implicitly for demo build/testing. No environment files or hosted data changed.
- Selected local demo database: Supabase project/container `compliancehub` / `supabase_db_compliancehub`, API `http://127.0.0.1:54321`, PostgreSQL port `54322`; observed 125 migrations through `20260904220000`. Dedicated fictional workspace will isolate showcase records.
- App port `3100` was not serving at initial inspection. Runtime versions: Node `25.6.1`, npm `11.9.0`.
- Do not run `scripts/test-db-isolated.sh` or `test-db-upgrade.sh`: both invoke resets. Fresh migration compatibility must use a newly created disposable database without resetting existing databases.
- Fresh verification: lint and typecheck exit 0; unit tests 281 files / 2,384 passed / 3 skipped; local pgTAP 93 files / 1,925 assertions passed. Logs: `artifacts/release-2026-09-05/{lint,typecheck,unit,database}.log`. Production build/browser evidence remains pending.
- The historical fixture `--verify` fails its guarded preflight; source remains unchanged. Diagnose without applying the fixture. The DB suite includes four concurrency suites with committed synthetic IDs and cleanup, so further full database runs will use a separate database; it is not wholly rollback-only.

- Historical verification recovered: copied only the missing proof artifact from the preserved checkout after matching SHA-256 `79e66498f58fee76966e9119fbd138911dbc8b2c8f5ac1775e423ed8e9449a30`; read-only verification passed with 30 records and zero added audit events. Fixture hash `d5eb0304d81e6ec00051490a405abe5c18b6d8f42f00f4c9bd0929f68a95454c`.
- Local pre-showcase backup: `artifacts/release-2026-09-05/local-before-showcase.dump`, SHA-256 `e3f0bec63005c5de7d57de0f33853f9945537fe2baa67b24d0ed0cfee60679c9`; archive listing readable (2,556 lines). This proves archive readability, not yet restoration.
- Scope correction: policy feedback threads/replies/resolution and leadership publication to workspace Members are present. Private feedback notifications, policy-to-control mappings, and selected external leadership recipients/revocation remain preserved unfinished ports; the earlier reporting check does not prove these shipped.
- Staging target clarification pending: historical Azure runbook names Supabase `ytenjiyjdcrjkgwmciqw`, current environment names `etpiqjbehbchkkzmbjzt`. No hosted writes approved.

- Release commit groups prepared: core `cbf48e5`, API/export `8a3738e`, native Jira + durable alerts `9e1ee69`, membership compatibility `c26b5d0`, retained verification tooling `349d671`. Native provider configuration and database credential-grant validation remain separate gates.
- Backup restoration exposed a real historical data-consistency issue: orphan `audit_events` for synthetic `72000000…` test organisations cause foreign-key recreation to fail. The existing concurrency test cleanup disables referential triggers and can leave these audit rows. No existing data was repaired/deleted; raw backup and partially restored diagnostic databases remain preserved. Full suite reruns must be confined to a separate database. Recovery is not yet certified.

### Active production rehearsal

- Demo tooling and evidence risk/task choices committed as `18babf170c830e238a3533c97e8555616413d186`. Focused tests: 5 files, 18 tests pass (launcher/setup guards plus Member/provenance/restoration evidence tests); typecheck and scoped lint pass.
- `npm run demo:build` succeeded; `npm run demo:start` runs the standalone production server at `http://127.0.0.1:3100`. `/api/health` returns `status: ok`, `db: ok`, release `18babf170c830e238a3533c97e8555616413d186`. Build metadata is `.next/local-demo-build.json`, applied migration `20260904220000`, API `http://127.0.0.1:54321`. This rehearsal includes two uncommitted setup-ordering lines and a pending DB test cleanup fix; it is not final exact-clean-release acceptance.
- Initial Browser inspection confirmed the production sign-in form renders. Showcase setup is creating the dedicated Northstar workspace through signup, application actions and assessment APIs. No real provider credentials or notifications are enabled.
- Diagnostic restored database `compliancehub_release_verify_20260905b` preserves the raw snapshot data, temporarily loaded the orphan audit FK as NOT VALID, then restored remaining objects. The initial restore stopped before its late archive permission grants; the archive does contain those grants. Resuming them restored application access, with one remaining Supabase GraphQL bootstrap-function mismatch. This diagnostic clone is not accepted as full clean-restoration proof.
- A narrowly scoped correction to test 071 cleanup removes only its exact synthetic organisation audit rows. Executing the cleanup portion in the diagnostic clone left zero orphan audit events and allowed FK VALIDATE to succeed. Original `postgres` database has not received this cleanup. Focused test 071 on the diagnostic clone passed 76 assertions. A separate correctly bootstrapped Supabase stack now provides the full-suite proof below.

- Fresh isolated Supabase project `compliancehub-release-1788565333-9540`, DB port `45402`, applied the complete migration chain and passed **93 files / 1,926 assertions**. Exact proof: `artifacts/release-2026-09-05/fresh-database-full.log`. The earlier helper output ending `NOTESTS` is preserved and explicitly rejected; the helper now requires positive file/test counts and PASS. Neither stack was reset.
- Browser found and reproduced mobile evidence overflow (390px viewport, 589px document width) and policy evidence mislabelled `Task: undefined`. Targeted rendering fixes are prepared; 3 evidence suites / 9 tests pass. Actual fixed production-browser verification is pending rebuild.
- Rehearsal build `82ec413` was rejected by the source identity guard because a script edit landed during compilation; it is not accepted. Clean build `9ae10be366ba19838ffdc203e335567eebd62eec` succeeded and is serving production on port 3100 (source hash `4fae81800d2c9f095b3b5ac83ec9e4cce0ee8c05d0f4f5ad6f260b2f955617e8`, local migration `20260904220000`). Later setup/test-only refinements are pending final release rebuild.
- Actual Browser on build `9ae10be`: evidence policy label reads `Policy: NS-POL-001: Northstar access review policy`; mobile viewport/document widths both 390px. Risk-register → treatment-task navigation shows the same owner, due date, risk and evidence. Audit and leadership report render within 390px. Finalised SoA/report verification is still pending setup completion.
- Clean isolated recovery proof: dump SHA-256 `15ce95131f9c626ba2b227e626af748fce75478a4c083ffcb767a6444ce31905`, zero orphan audit events. Restore into **new** DB `release_restore_20260905b` in the retained fresh container required login as local `supabase_admin`, restoration of the source GraphQL bootstrap wrapper, then remaining ACL/default grants/event triggers. All **117** public/auth/assessment/migration tables match exact row fingerprints. Proof files `fresh-{source,restored}-data-fingerprints.txt`, `graphql-bootstrap.sql`, `fresh-restore-finish.{list,log}`. This proves a clean local database restoration with explicit bootstrap steps; it does not repair/certify the original historical dump or a hosted backup.
- Local resource issue observed: Realtime restarted repeatedly (24 observed restarts, browser WebSocket 502) while three temporary test projects shared a 2GB Colima VM. Completed temporary auth/Kong services and two failed-start test databases were stopped without deletion; the main demo and retained fresh regression database remain running. Verify Realtime stability before final browser acceptance.

### Showcase and security acceptance

- Northstar Demo — Showcase v1 (`2960769e-ff23-427a-9d66-eec278518882`) now has one completed assessment with 10/10 answers, 93 reviewed SoA controls and an immutable v1, one medium risk, one owned treatment/task, two current evidence records with 97 links, one approved policy, one audit/checklist/finding and one published leadership snapshot. Normal setup rerun and verify-only preserve exact IDs, counts and row fingerprints. `showcase-idempotency.json` records unchanged historical CEO rows, fixture source and proof artifact. Private local manifests and sessions remain ignored under `artifacts/showcase-v1/`.
- Build `9ae10be` browser rehearsal demonstrated the connected journey, correct evidence labels and mobile width; Member tests passed on desktop and mobile. Harness-only failures (inherited owner cookies in the anonymous context, ordinary cancelled navigation/download requests) were corrected. Final exact-version run remains pending. Throttled browser showed **Loading monitoring** before rendering; unknown risk showed Page not found and recovery navigation, unknown SoA PDF returned 404. Evidence: `loading-error-proof.json`, `member-browser-proof.json`, screenshots and retained browser logs.
- SoA PDF/DOCX labels now explain that immutable snapshots contain manual references, while linked evidence remains in the workspace vault. Existing snapshot contents are preserved. Earlier PDF page renders were checked; updated final export rendering remains pending.
- Jira gap reproduced: ordinary/anonymous roles could execute three credential RPCs. New migration `20260905010000` restricts each exact signature to service_role. Real-role regression: before 15/21 failed, after 21/21 passed. No actual credential contents were read. Logs `jira-credential-grants-{before,apply,after}.log`.
- Auditor RPC now reserves atomic database budgets before access logging/aggregation: 300 calls/minute globally and 30/minute per issued token. Null/empty/over-256-byte tokens are rejected early; unknown tokens cannot create arbitrary per-token rows. Exhaustion returns null so its counter transaction commits. Exact old function replay failed 8/17 assertions; new and compatibility tests passed 3 files / 39 assertions. Logs `auditor-old-function-repro.log`, `auditor-limits-after-local.log`. Limits do not provide pagination, retention, or upstream network-level denial-of-service protection.
- Observability now fails closed with 503 when durable counters fail, return invalid values, or lack configuration; no fallback or error writes occur on that path. Normal exhaustion returns 429; legitimate reports still work. Real limiter + route regression and existing tests: 3 files / 26 pass. Other callers retain their existing fallback behavior. Independent Astra candidate review found no actionable regression; reviewer independently executed those 26 tests, but did not independently rerun SQL/concurrency tests.
- Both security migrations applied only to identified LOCAL main and isolated test stacks, now 127 migrations through `20260905011000`. Hosted databases remain unchanged. Migration logs preserved.
- Browserslist updated 4.28.4 → 4.28.9 with required browser-data dependencies, resolving [query-cache advisory](https://github.com/advisories/GHSA-c83g-rgw3-j3cx) and [custom-statistics advisory](https://github.com/advisories/GHSA-73wf-gq98-2v4g). Final npm audit: 0 high/critical, 2 moderate package entries (ExcelJS and its UUID dependency, one underlying [UUID advisory](https://github.com/advisories/GHSA-w5hq-g745-h8pq)). ExcelJS's identified calls use UUID v4, which the advisory explicitly excludes. A forced major override was not adopted; this remains a dependency limitation requiring upstream tracking, not a claim of a clean audit.

- Final source gate: lint/typecheck pass; **286 unit files / 2,415 passed / 3 skipped**, **95 SQL files / 1,964 assertions passed** on the isolated database after all 127 migrations. Exact logs `final-{lint,typecheck,unit,database}.log`. No full-suite replay was performed on the historical main database.

### Staging approval packet — blocked target, no hosted changes

- Intended runbook target: Azure app `ca-compliancehub-staging`, resource group `rg-compliancehub-staging-uks`, UK South; hosted Supabase runbook ref `ytenjiyjdcrjkgwmciqw`. Current `.env.local` instead names `etpiqjbehbchkkzmbjzt`. Read-only Azure lookup returned ResourceGroupNotFound in the authenticated subscription; unrelated apps are not substitute targets. Exact destination, live release/image/revision and backup remain unverified. Target clarification remains pending.
- Proposed database changes, subject to exact hosted migration inventory/dry-run: nine ordered migrations after `20260825124025`: `20260904201754`, `20260904201759`, `20260904210402`, `20260904210426`, `20260904210742`, `20260904220000`, `20260905010000`, `20260905011000`, `20260905012000`. They cover assessment/treatment workflow guards, Jira enum/lifecycle, durable alert delivery, membership compatibility the two security fixes and backend-only export audit appends. No hosted list/dry-run/application has been claimed.
- Before approval: identify target and canonical origin, capture the active image digest/revision/traffic and secret-reference names, verify a recoverable hosted backup and restore procedure, compare schema history and dry-run, and replace the deployment workflow's old migration attestation only with the reviewed target checkpoint. Native Jira stays unavailable unless its server-only client ID/secret are deliberately configured; existing Azure secret mapping does not include them. Outbound Slack/email/provider delivery remains disabled for this demo.
- Deployment approval must name the exact tested release SHA/image, Azure target, Supabase project and ordered migrations, expected downtime and rollback predecessor. The existing automatic main workflow is not used: no push/merge/deploy is authorized. After approval, migrate in order, verify grants/schema, deploy the immutable version, require health `status: ok`/`db: ok` with exact SHA and migration, then repeat the connected journey and Member/anonymous checks at the canonical staging URL.
- Rollback: route traffic back to the captured healthy immutable revision with its matching secret references, confirm its health/release and critical journey. Container rollback does **not** reverse database migrations; these changes require compatibility verification with the predecessor. If database recovery is necessary, restore the verified backup into a separate environment and validate before any approved cutover. Never run destructive down-migrations or restore over existing data as an automatic recovery action. Current predecessor and hosted recovery are unknown, so deployment is blocked.

- Acceptance caught a production-only export audit failure despite the four browser tests passing: `service_role` lacked INSERT on `audit_events`. Migration `20260905012000` grants append only; ordinary users cannot forge events, UPDATE/DELETE remain ungranted and immutable triggers remain active. Regression reproduced 4/8 failing assertions before and 8/8 pass after. Four real export downloads changed the fictional workspace export-event count from 0 to 4. Independent review found no actionable concern and independently passed 4 export unit tests. Focused lint/typecheck pass. Both local databases now have 128 migrations; no hosted change.
- Actual Browser rechecked assessment 10/10, all 93 SoA controls reviewed, the linked medium risk/owned December-31 task/evidence, tested audit and report (89% readiness, 1 task, 2 current evidence, 1 medium risk, 1 audit). SoA PDF first-page rendering verifies the manual-reference explanation and preserved snapshot content. Main Realtime has remained at restart count 25 since 00:09:27 UTC after test-service memory pressure was removed.

### Reproducible local release

- Release reference: annotated tag **`demo-rc-20260905-2`**. Resolve its exact commit with `git rev-parse demo-rc-20260905-2^{commit}`. The tag includes the final code and these operator documents; no main merge, push or deployment is part of acceptance.
- Final application source `e8a806be483fa77c54f8e68629adeb6ea3f30cb2` passed production compilation and all **4 connected desktop/mobile and Member browser tests** (`export-fixed-showcase-e2e.log`), with no export audit errors. Restarting that same standalone build preserved the session/data and returned healthy DB/release identity. Throttled loading, recovery navigation, refresh, missing-export 404 and actual DOCX download passed (`final-boundary-proof.json`). The documentation-only release commit is rebuilt and checked again; authoritative final SHA/environment/results are saved in `artifacts/release-2026-09-05/release-acceptance.json` and `.next/local-demo-build.json`.
- Test totals are intentionally not conflated: the full baseline after security fixes passed 2,415 unit tests and 1,964 SQL assertions; the subsequent append fix passed its focused **4 unit / 8 SQL** checks, lint/typecheck, production build and four browser tests. The three skipped baseline unit tests remain skipped. Full expensive suites were not repeated for documentation.
- Startup, five-minute walkthrough, private synthetic credential location and recovery instructions: [demo-release.md](demo-release.md). Demo URL `http://127.0.0.1:3100`; database `compliancehub` at `http://127.0.0.1:54321`, migration `20260905012000` (128 applied migrations). Staging remains **unverified and not deployed** pending exact target, backup/dry-run evidence and explicit approval.

### Staging state verified after local release — 2026-09-05

This section supersedes the earlier **unverified** target conflict and hypothetical
nine-migration packet. It is staging preparation after the immutable local tag;
it does not alter or invalidate the accepted local application artifact.

- Saved GitHub repository and `azure-staging` environment variables agree on Azure app `ca-compliancehub-staging`, resource group `rg-compliancehub-staging-uks`, environment `cae-compliancehub-staging-uks`, canonical origin `https://ca-compliancehub-staging.calmrock-cf8a542f.uksouth.azurecontainerapps.io`, and Supabase `ytenjiyjdcrjkgwmciqw` (**ComplianceHub MCP Staging**, active, eu-west-1). This resolves the `.env.local` conflict: `etpiqjbehbchkkzmbjzt` is a different project. Sanitized target variables/project metadata are saved in the release artifacts.
- Azure has two cached subscriptions. The current AdTecher subscription lacks the configured resource group. Read-only access to the other subscription (`5040284c-9282-4bc4-84ed-ac89fb397af8`) is blocked by Microsoft security defaults (AADSTS530035); interactive re-authentication is required. No account context or Azure resource changed. The canonical health URL timed out after 20 seconds; no current deployment health or rollback predecessor is claimed. Historical successful deployment run `32028274117` at SHA `317cdc1f2b617abf895c6159ec9a87041de4386b` proves only historical deployment.
- Actual hosted migration inventory is **77**, with historical `staging_...` names and generated version IDs. Every stored SQL body matches its corresponding local migration file exactly (77/77 MD5 equality, used solely for byte comparison). **51 local release migrations are absent by mapped history**, including 12 older AI/scope/automation migrations. Schema-only checks also confirm the expected AI, scope, automation, GitHub, native Jira and durable-alert tables are absent. Do not apply the earlier nine-migration assumption or blindly push all local version IDs. Canonical inventories: `staging-migration-reconciliation.json`, `staging-migration-hash-comparison.json`, `staging-schema-presence.json`.
- The isolated local schema-only upgrade rehearsal passed: all 77 historical migrations followed by all 51 missing migrations, 128 applied/zero pending; **96 files / 1,972 SQL assertions PASS**. Project `compliancehub-staging-1788569562`, DB port 47012; containers stopped with volumes retained. The first incorrectly ordered startup attempt is preserved and rejected. Proof: `staging-upgrade-{rehearsal.json,baseline.log,pending.log,full.log,migration-list.log,stop.log}`. No hosted business rows were copied into this rehearsal and no existing database was reset.
- Protected staging secret **names only** were inspected: six core/Azure names exist. No GitHub or Jira credential aliases, Slack destination hash, or hosted migration attestation are configured in the inspected environment/repository. Current deployment preflight therefore cannot pass. Secret values were neither retrieved nor changed. The environment still has only a branch rule, with no required reviewer gate.

- Hosted backup inventory returned `backups: null`, empty physical-backup data and `pitr_enabled: false`. This supplies **no verified restore point**; it is not proof that a recoverable backup exists. A recoverable hosted backup plus a separate-environment restore remains a deployment gate (`staging-backups.json`).
- A clean source archive for the immutable local release is prepared: `ComplianceHub-demo-rc-20260905-2.tar`, SHA-256 `241f981a394746833cb54f6a14e44216d7a372e22b955c9bc7c6e629d723b9df`. It comes from `git archive`, excludes ignored local credentials/dumps/sessions and preserves exact tagged source. Use this clean source context for a future staging image; the current working directory contains preserved private artifacts that should not enter a build context. No image was pushed or deployed.

- Aggregate hosted preflight (no business values returned): tasks, monitoring findings, monitor sources, integration connections and invitations have zero rows; memberships has five. There were zero waiting locks, no other open transaction and no automation-marker task duplicates at observation time. These checks are point-in-time and must be repeated before execution (`staging-aggregate-preflight.json`).
- Independent migration review found that the historical app's monitoring upsert is incompatible with the new finding uniqueness constraint; its image is not a verified rollback. Missing older migrations also temporarily replace hardened SoA finalisation, and the current image requires native Jira schema absent at the old bridge checkpoint. Consequently the old bridge/final runbook cannot be executed as written. A transaction-based alternative with precommitted enum additions is under review; no hosted packet is approved.
- Hosted logical schema, data and role exports now exist privately under `staging-backup-{schema,data,roles}.sql` (mode 0600), with checksums. The initial concurrent schema dump failed CLI temporary-login authentication; its successful sequential retry is recorded separately. Do not run concurrent linked CLI dumps that rotate the same temporary login. Restore into a new isolated database is in progress; backup recovery remains unproven until that succeeds. The 89 source table fingerprints cover 834 rows; storage metadata has one bucket and zero objects, so no stored object files are omitted for this observed snapshot. No values from auth/business rows are exposed in the evidence report.

- The proposed enum-prelude/49-file transaction design passed independent static review: all three enum types already exist in the 77 baseline; the other files contain no additional enum additions, explicit transaction boundaries or identified transaction-prohibited statements. A failed final transaction should leave exactly 79 migration records and the original hardened SoA function; successful commit should expose all 128 records and final Jira grants together. Exact-runner success/failure rehearsal remains in progress; the existing bridge workflow is not being reused or silently relaxed.
- Read-only transport probe through `supabase db query --linked --file` accepted a 1,520,336-byte query containing `BEGIN READ ONLY`/`COMMIT`, returned transaction read-only=true and PostgreSQL 17.6 with a two-minute statement timeout. This proves the file transport accepts a larger body than the proposed packet, not that DDL will finish or that a timeout means rollback (`staging-readonly-transport-probe.log`).
- First backup-restore attempt was rejected: the historical local configuration uses PostgreSQL 15 while hosted staging is PostgreSQL 17; the attempt also used insufficient restore ownership. That failed empty database is preserved. A new isolated PostgreSQL 17 restore with the proper managed administrator is underway; no dump setting is being removed merely to force compatibility.

- A later bounded retry reached the canonical staging health endpoint: HTTP 200, `status: ok`, `db: ok`. Its response has **no release SHA, Slack policy or reservation-mode markers**, so it proves the old endpoint/database are reachable, not that the demo release is deployed or that the rollback gate passes. The earlier timeout remains preserved (`staging-health-retry-{headers.txt,body.json}`).

- Complete logical application restore is now verified on isolated PostgreSQL 17.6: **90/90 tables and 835 rows match**, including the previously omitted private configuration table; 185 foreign keys and 192 CHECK constraints pass, 443 ACL/207 ownership/141 policy statements match. All backup inputs are private, checksummed and retained. The restore container has no network or published ports. This proves logical application recovery, not managed-service configuration, encryption root-key recovery or an Azure cutover. Canonical evidence: `staging-backup-restore17-full.json`, `staging-backup-restore17-full-runbook.md`.
- The initial 77→79 enum prelude and 49-file transaction rehearsal reaches its final assertions, deliberately fails with SQLSTATE `ZX001`, and preserves 79 history rows/original SoA/no Jira functions. Successful execution reaches 128. This is **not final approval evidence**: it used `supabase_admin` rather than the hosted `postgres` role, and psql interpolation established a same-session transaction but not a literal single-query submission. Corrected rehearsal will use direct `postgres` login and `pg_read_file(...) \gexec`, plus PostgreSQL 17's whole-transaction timeout.
- Actual restored staging permissions exposed a gap hidden by the fresh-source baseline. Direct anonymous/authenticated grants survive the older PUBLIC-only counter revocation. `audit_events` also retains broad managed-platform grants, including backend TRUNCATE, which bypasses its UPDATE/DELETE trigger. Anonymous audit writes remain blocked by row policies, but the excess grants violate the intended boundary. Read-only hosted privilege checks confirm the drift. Existing focused tests reproduce auditor 16/17, Jira 21/21, export 5/8; the added complete privilege matrix reproduces 17/37 failures. A narrow additive migration and role-correct regression are in progress. No hosted change has been made; the old accepted tag remains preserved.


### Staging-derived permission correction and next candidate

- Source commit `25171956ee1f74cbd1a55e8da539accc7f22ef50` adds only migration `20260905013000` and relevant regressions. It removes direct anon/auth counter execution and normalizes audit table/identity sequence capabilities. The previous tag and acceptance evidence are preserved. The next local candidate is `demo-rc-20260905-3`, checkpoint **129 migrations**; production rebuild/browser acceptance for its final documentation commit follows.
- Focused regression passes on both local PostgreSQL 15 and staging-derived PostgreSQL 17: **5 files / 102 assertions**. This includes real generated-identity export inserts, trigger-generated task auditing, own-tenant audit reads, other-tenant exclusion, direct counter rejection, anonymous auditor budgets, Jira grants and denied TRUNCATE/sequence privileges. Independent Astra review found no surviving bypass or regression in scope and independently ran the PG17 assertions; its direct PG15 attempt lacked the temporary pgTAP extension, while the parent's supported local runner passed all 102. Evidence: `privileges-local-focused.log`, `privileges-role-correct-tests.json`.
- Corrected literal-query rehearsal, using current/session user `postgres`, PostgreSQL 17.6 and the actual hosted schema/default grants, passed **77→79→129**. Both enum changes commit first; all remaining **50** original migrations plus history rows and final privilege/function/constraint assertions commit together. Deliberate `ZX001` failure after all final assertions preserved 79 rows and the original SoA hash with no Jira function. The successful main packet took 0.226 seconds locally. Transaction timeout is 90 seconds, per-statement timeout 60 seconds and lock timeout 5 seconds; local timing does not promise hosted duration. Exact constraint definitions were independently derived from source and pinned by digest. Evidence: `atomic129-rehearsal.json`, packet SQL/hash manifest and separate success/failure logs.
- Additional function grant differences remain documented in `staging-function-permission-diff.json`. Beyond the fixed counter, bounded static inspection found existing caller/tenant checks blocking anonymous mutation in ten extra anonymously executable functions. One policy-reaccept helper distinguishes a missing from an inaccessible supplied policy UUID; no policy content or notification mutation was demonstrated. Complete ACL parity or a new full security audit is not claimed. This does not waive the staging journey and permission checks.
- All hosted work remains read-only. Azure re-authentication is still required to capture the current immutable image/revision, secret references, traffic and a valid recovery/cutover plan. The historical image cannot simply be restored against the upgraded monitoring schema. Do not use the old bridge workflow, push/merge main, apply either hosted packet or deploy without a concrete target/change summary and explicit user approval.

- Final next-candidate acceptance now completed: tag **`demo-rc-20260905-3`**, exact commit **`11a8fd5e5a4666051c346b097350468b915a4347`**. Production build, all four connected desktop/mobile and Member browser checks (18.8s), verify-only showcase consistency, same-build restart, throttled loading/recovery, refresh, missing-page/export error handling and real DOCX export pass. In-app Browser confirms readable audit activity and unchanged report totals after restart. Final exact-source PG17 transaction success/rollback rehearsal and 102 focused assertions pass. Evidence: `release-acceptance-rc3.json` (also current `release-acceptance.json`), `rc3-*` logs and source archive. Prior `release-acceptance-rc2.json`, tags, backups and failed-attempt evidence remain preserved. Offline restored-data containers are stopped with their volume retained; the local demo remains running. This post-tag checklist entry records acceptance and ongoing staging work; it does not change the immutable tested commit.

- Staging handoff consolidated in `artifacts/release-2026-09-05/staging-handoff.md`: exact candidate/source archive and packet hashes, 52 missing migrations (2 enum +50 atomic), timeout/checkpoint recovery rules, backup scope and remaining Azure/configuration/approval gates. All three packet files and seven private backup inputs were rechecked against their hashes/lengths, and backup mode0600 was verified (`staging-handoff-integrity.json`). Azure access still returns AADSTS530035; the local health endpoint still reports the accepted release. No hosted mutation or deployment was attempted.

- Blocked audit: Azure AADSTS530035 recurred across three consecutive goal turns, including a fresh read-only target lookup. All independent staging preparation is recorded in the handoff; live revision/configuration and a valid cutover/recovery approval cannot be completed without interactive sign-in. Goal marked blocked pending that user action. The local demo remains healthy at `11a8fd5e5a4666051c346b097350468b915a4347`; no hosted mutation occurred.

- User-requested live status check at 2026-09-05 10:26 UTC: a new browser session successfully signed in with the fictional showcase credentials, opened dashboard/completed assessment10/10/audit activity/live leadership report, and refreshed while authenticated. Local health identifies exact RC3 SHA. Current sign-in error was traced to provider HTTP400 Invalid login credentials; no reset or code change was needed. Hosted Azure health returned status/db ok and its sign-in page rendered in Browser, but its authenticated journey/release remains unverified. Evidence: `current-signin-check.json`, `current-app-report.png`.
